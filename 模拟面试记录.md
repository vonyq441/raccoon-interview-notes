# 模拟面试记录

> 本文档按面试日期记录问题、回答建议和复盘结论。只有在用户明确要求“整理到文档”或“追加到面试记录”时才新增内容，普通答疑和模拟面试过程不自动写入。

## 2026-09-17｜华为实习项目

### 第 1 题：一条测试需求如何形成可交付的 AW

**问题：** 你在华为项目中开发了 AW 检索复用与代码生成工具。请从“一条新的测试需求进入系统”开始，按照实际执行顺序，完整介绍这条需求如何经过检索、复用判断、代码生成和结果校验，最后形成可以交付的 AW。

#### 这道题考什么

面试官不是只想听“使用 Agent 搜索并生成代码”，而是要确认你是否真正理解完整工程流程：系统接收什么输入、怎样避免漏检已有能力、凭什么决定复用或新建、生成后如何验证，以及最终交付什么内容。

#### 推荐回答

测试人员收到新需求后，首先会把需求转化为测试用例，并确定各个测试步骤需要哪些 AW。AW 是用 Python 封装的业务能力，可以被不同测试脚本重复调用。传统流程比较依赖测试人员经验，容易遗漏已有 AW，造成重复开发；现有脚本生成工具在缺少相似样本时，也可能生成不符合业务逻辑的代码。

我的工具以测试需求、测试步骤和相关上下文作为输入。第一阶段先对需求进行初步建模，提取业务目标、操作对象、输入参数和预期结果，再从代码仓库、Wiki 和 API 文档中召回候选 AW。第二阶段针对候选项定向补证，继续查找函数定义、调用示例和业务说明，避免只根据名称或关键词作出判断。

取得证据后，系统会比较候选 AW 与需求在业务语义、接口参数、输入输出、副作用和失败行为等方面的一致性。如果已有能力满足需求，就进入 REUSE 分支，输出推荐 AW、源码位置、匹配依据、调用示例以及已知风险；如果没有可直接复用的能力，则进入 CREATE 分支，参考相似 AW 的代码结构和设计方式，以 Append-Only 方式追加新能力，避免修改并破坏已有 AW。

生成完成后，系统会检查需求覆盖情况、接口参数一致性、证据来源和 Python 语法，并给出新增代码位置及调用示例。最终再使用真实测试用例进行人工验证，确保结果能够被测试人员审查、调用和交付。

#### 速记流程

```text
测试需求与步骤
      ↓
需求建模：目标、对象、参数、预期结果
      ↓
初步搜索：代码仓库、Wiki、API 文档
      ↓
定向补证：源码、调用示例、业务说明
      ↓
复用判断：语义、参数、输入输出、副作用、失败行为
      ↓
REUSE 已有 AW  /  CREATE 新 AW（Append-Only）
      ↓
需求覆盖、参数一致、证据溯源、Python 语法检查
      ↓
真实用例人工验证并交付
```

#### 容易失分的点

- 不要把“测试脚本”和“AW”混为一谈：AW 是可复用的业务能力，测试脚本负责按照测试步骤编排和调用 AW。
- 不要只说“检索到就复用”：必须说明复用判断至少比较业务语义、参数、输入输出、副作用和失败行为。
- 不要把“生成报告”当作流程终点：还需要需求覆盖、参数一致性、证据溯源、语法检查和真实用例验证。
- 面试表达中不必强调用特殊标签要求模型思考。更稳妥的说法是：通过结构化检查清单、固定输出格式、参数确认和生成前约束，提高结果的稳定性与可审查性。

#### 回答评价

当前回答已经能说明业务背景、两阶段检索、REUSE/CREATE 分支和交付报告，整体达到校招合格偏上的水平。进一步提升的关键是压缩背景，把更多时间用于说明“如何判断可以复用”和“如何证明生成结果可交付”。

---

## 2026-09-18｜华为实习项目

### 第 2 题：参数不同且存在副作用时，如何判断 REUSE 或 CREATE

**问题：** 假设系统检索到一个已有 AW，它的业务功能与新需求基本一致，但参数定义不同，而且执行后可能产生额外的业务副作用。系统依据什么标准判断它能否直接复用？如果不能直接复用，又会如何处理？

#### 回答评价

回答能够讲清“初步建模—定向补证—主 Agent 决策”的流程，也考虑了参数覆盖、副作用调查和低置信度时的人工裁断，整体达到校招合格偏上的水平。需要进一步明确复用标准，并避免把 CREATE 描述成直接修改已有 AW。

#### 推荐回答

系统不会仅凭代码相似度决定是否复用，而是从业务语义、接口参数、输入输出、副作用和失败行为几个维度进行判断。初步建模阶段如果发现参数不一致或存在额外副作用，子 Agent 会生成定向补证问题；主 Agent 再委派子 Agent 查询函数定义、调用示例和业务文档。

如果差异只是参数名称、格式或默认值不同，并且可以通过参数映射解决，同时不会改变业务语义，就继续复用已有 AW，并在 REUSE 报告中给出参数映射和调用示例。如果已有 AW 会产生需求不允许的副作用，例如额外修改数据或改变环境状态，就不能直接复用。证据不足时交给测试人员确认，而不是让模型自行猜测。

确认无法复用后进入 CREATE 分支。系统参考最接近的 AW 的代码结构和设计规范，以 Append-Only 方式新增 AW，不修改已有能力。生成后再检查需求覆盖、参数一致性、Python 语法和调用示例，并通过真实测试用例完成人工验证。

#### 判断标准

- **业务语义：** AW 实际完成的业务动作是否与需求一致。
- **接口参数：** 名称、类型、单位、必填项和默认值能否兼容或安全映射。
- **输入输出：** 返回内容和数据格式是否满足后续测试步骤。
- **业务副作用：** 是否修改数据库、删除数据或改变测试环境状态，需求是否允许。
- **失败行为：** 异常、超时、重试、幂等和回滚行为是否符合预期。

#### 速记结论

> 参数能够安全适配、业务语义不变且副作用可以接受，就复用；否则以 Append-Only 方式新增 AW，不修改已有能力。

### 第 3 题：AW 生成结果 95% 是如何评测的

**问题：** 简历中写到“AW 生成结果准确率达到 95%”。这个 95% 是如何定义和计算的？使用了什么评测数据、判定标准和基线方案，又如何避免样本量太少或人工判断过于主观，导致指标缺乏可信度？

#### 回答评价

回答能够说明测试集构造方法、CREATE 与 REUSE 两类场景，以及 `19/20=95%` 的具体来源，证明该数字不是主观估计。主要不足是样本量较小、两类任务共用了一个指标、判定标准不够量化，而且没有完成严格的同批样本基线对照。

#### 推荐回答

这里的 95% 更准确地说是小规模离线测试中的整体任务通过率。测试集一共包含 20 条样本，其中 10 条用于验证 CREATE，另外 10 条用于验证 REUSE。

我先根据业务场景和复杂度对已有 AW 进行分层，再人工确认样本。对于 CREATE 样本，我在复制的代码仓库中删除目标 AW 的实现，并将它的业务需求改写成自然语言输入；对于 REUSE 样本则保留已有 AW，检查工具能否正确检索并推荐它。测试开始前，我根据原始实现记录业务语义、接口参数、输入输出和副作用等关键要点，作为统一判定标准。

工具依次处理这 20 条需求。REUSE 场景主要检查是否推荐了正确 AW、源码位置和调用方式；CREATE 场景则检查需求覆盖、参数一致性、输入输出、副作用、Python 语法以及代码是否能够被实际调用。最终 20 条样本中有 19 条通过，1 条生成结果的参数设置不符合需求，因此整体任务通过率是 `19÷20=95%`。

不过，这个结果只能说明工具在当前小规模测试集上的可行性，样本量还不足以证明生产环境中的稳定准确率。当时也没有完成严格的同批样本基线对照，所以不能直接宣称相对原方案提升了多少。后续应扩大样本，并分别统计 REUSE 判断准确率和 CREATE 代码通过率。

#### 指标应该怎样拆分

- **REUSE 判断准确率：** 是否识别出应该复用，并推荐了正确的已有 AW。
- **CREATE 生成通过率：** 新生成 AW 是否覆盖需求并通过接口、语法和运行验证。
- **整体任务通过率：** REUSE 与 CREATE 样本中最终能够正确交付的比例。
- **人工效率指标：** 单条任务耗时、人工修改次数和首次通过率。

#### 容易失分的点

- 不要只说“准确率达到 95%”，应说明“20 条离线样本中有 19 条通过”。
- 不要把 AI 选样描述成严格随机抽样；更稳妥的说法是按业务领域和难度分层，再由人工确认覆盖范围。
- CREATE 测试不能只删除函数实现，还要排查调用样例、文档或缓存是否直接暴露目标答案，避免测试集泄漏。
- 如果没有做严格基线实验，就如实说明，不能根据单组结果宣称相对原方案提升了多少。

#### 速记结论

> 95% 是 20 条离线样本中 19 条通过的整体任务通过率，用于验证方案可行性；它不是经过大规模统计验证的生产准确率，后续还应扩大样本并拆分 REUSE 和 CREATE 指标。

### 第 4 题：多阶段检索为什么能降低上下文占用

**问题：** 为什么子 Agent 和“初步建模—定向补证”能够减少主 Agent 的上下文占用？子 Agent 最终向主 Agent 返回哪些内容？又如何防止压缩上下文后遗漏关键证据，导致主 Agent 作出错误的 REUSE/CREATE 决策？

#### 回答评价

回答已经说明主 Agent 先构建 AW 初步画像，再由子 Agent 扫描代码仓库和 Wiki，最后返回结构化报告；也能解释两阶段检索如何减少无目的搜索。需要进一步明确上下文隔离原理、报告字段、防遗漏机制以及“降低约 50%”的测量口径。

#### 推荐回答

主 Agent 收到需求后，先提取业务目标、操作对象、接口参数、预期输出和所属领域，形成 AW 初步画像，然后将检索任务委派给子 Agent。

子 Agent 在独立上下文中扫描代码仓库，并按需查询 Wiki 和 API 文档。搜索过程中产生的大量文件内容、失败查询和无关候选不会进入主 Agent 上下文。子 Agent 最终只返回结构化报告，其中包括候选 AW、文件路径、函数名和行号、参数及行为摘要、匹配与不匹配项、证据来源、置信度和待补证问题。

主 Agent 收到报告后，不需要重新遍历整个仓库，而是针对排名靠前的候选读取关键源码，并围绕参数、副作用或失败行为等未解决问题发起定向补证。因此，第一阶段负责尽量召回候选，第二阶段负责验证关键细节。

为了避免结构化压缩遗漏证据，报告采用固定证据契约。业务语义、参数、输入输出、副作用和失败行为必须附带源码或文档位置；字段缺失、证据冲突或置信度不足时，不能直接进入 REUSE，而是继续补证或交给测试人员确认。主 Agent 还会重新读取决定性源码，不会只依赖子 Agent 的摘要。

上下文降低约 50% 是通过同类任务的工程对比得到的。原流程会把大量搜索结果持续放入主上下文，在 200K 窗口中通常只能连续处理约 4 个任务；优化后主上下文只保留需求和结构化证据，至少可以连续处理 8 个任务。因此该数据是实际观测到的近似结果，不是严格统计意义上的精确值。

#### 子 Agent 返回的结构化报告

- 需求画像与检索范围；
- 候选 AW 列表及排序；
- 仓库、分支或 Commit、文件路径、函数名和行号；
- 参数、输入输出、副作用和失败行为；
- 与需求的匹配项和不匹配项；
- 支持结论的源码或文档证据；
- 置信度、证据冲突和待补证问题。

#### 防止遗漏的机制

- 每项关键结论必须附带可定位的证据，不能只返回自然语言结论。
- 固定检查语义、参数、输入输出、副作用和失败行为，缺项不能直接 REUSE。
- 低置信度、证据冲突或候选为空时触发定向补证和查询扩展。
- 主 Agent 对影响决策的源码进行二次读取和验证。
- 仍无法判断时请求测试人员裁断，不让模型自行猜测。

#### 速记结论

> 子 Agent 隔离大规模搜索过程，主 Agent 只保留需求和可定位的结构化证据；再通过固定证据字段、定向补证和源码复核控制信息压缩造成的遗漏风险。
