{
  "name": "raccoon-interview-site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-interview.mjs",
    "predev": "npm run sync",
    "dev": "vitepress dev docs --host 127.0.0.1",
    "prebuild": "npm run sync",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
