# 中央调度平台设计与面试准备

> 编写日期：2026-09-10。本文结合 HxSay「多级复杂智能巡检中央Agent开发」目录的 12 篇正文及 1 篇索引、智能巡检工程、`bot_mind / g1_base` 源码和你提供的简历整理。
>
> **定位：以真实机器人项目为基础的完整扩展设计，不是新增平台已经交付的证明。** 下文默认按目标能力讲解，新增类、表、接口均为建议设计。原项目部署验收属于你提供的经历；中央 Agent、pgvector、可靠指令协议等不能顺带认定为当时已上线。本文没有实现代码，也不发布到网页。

## 目录

- [一、先用一个例子理解项目](#一先用一个例子理解项目)
- [二、真实基础与扩展边界](#二真实基础与扩展边界)
- [三、课程十三篇内容怎样迁移](#三课程十三篇内容怎样迁移)
- [四、整体架构与技术选型](#四整体架构与技术选型)
- [五、一次接待任务的完整流程](#五一次接待任务的完整流程)
- [六、多机器人调度与控制权](#六多机器人调度与控制权)
- [七、任务状态与可靠执行](#七任务状态与可靠执行)
- [八、上下游接口与协议](#八上下游接口与协议)
- [九、语音问答与中断续讲](#九语音问答与中断续讲)
- [十、RAG知识库设计](#十rag知识库设计)
- [十一、项目结构与核心类](#十一项目结构与核心类)
- [十二、数据库与缓存](#十二数据库与缓存)
- [十三、安全与异常处理](#十三安全与异常处理)
- [十四、部署测试与迭代](#十四部署测试与迭代)
- [十五、项目介绍与职责表达](#十五项目介绍与职责表达)
- [十六、面试题与参考回答](#十六面试题与参考回答)
- [十七、证据索引与待确认事项](#十七证据索引与待确认事项)

## 一、先用一个例子理解项目

### 1.1 原来怎么用

根据你的回忆，原项目已经有 Java 平台：工作人员在手机上选机器人、选任务点，再点击开始讲解。机器人端负责实际导航、播报和动作。

因此不是“从没有平台开始建设”，而是把**人工逐项操作的平台，升级成可以理解接待意图、形成计划并跟踪结果的平台**。

例如工作人员说：

> 今天下午三点，在一楼接待一组访客，重点讲液冷机柜和具身智能，控制在十五分钟左右，选一台空闲 G1 机器人。在具身智能板块安排它和机器狗互动，让 G1 出声命令机器狗做出拜年动作。

中央平台应该做的是：

1. 理解时间、楼层、讲解主题和时长；缺少必要信息就追问。
2. 查真实展项、点位、讲解模板，以及 G1 和机器狗的可用状态、能力与时间安排。
3. 生成 G1 带队讲解、机器狗准备互动两条有关联的子任务，明确互动开始前双方必须满足的条件。
4. 把计划展示给工作人员确认，而不是立即让机器人走动。
5. 到执行时间重新检查资源，分别选择承担讲解的 G1 和支持拜年动作的机器狗。
6. 下发业务任务，等待双方准备完成，再按“G1 口令播报完成 → 机器狗拜年 → 动作完成后 G1 继续讲解”的依赖推进。
7. 只在必要步骤真正完成后结束接待；失败则给出可执行的处理选项。

这个例子是设计情景，不代表真实发生过这场接待，也不代表这两个展项的位置、路线顺序已经核实。

**职责边界：中央平台负责解析任务、规划业务步骤和分配/协调任务，不规划机器人导航路径，不计算避障轨迹，也不输出运动控制量。G1 的导航与避障由 g1_base 承担。** 下文的执行跟踪是判断何时可以下发下一项业务任务，不是中央接管底层运动控制。机器狗使用自己的端侧控制服务；其接口及拜年能力需要另行核实，不能假设 g1_base 已经控制机器狗。

### 1.2 三个容易混淆的概念

| 概念 | 通俗理解 | 在本方案中的职责 |
|---|---|---|
| 中央 Agent | 能理解需求、查资料、提出方案的接待助理 | 自然语言解析、知识查询、计划建议和解释 |
| 调度器 | 按明确规则分配工作的值班主管 | 排队、选机器人、占用资源、执行状态推进 |
| 机器人控制层 | 真正做动作的执行系统 | 导航、运动互斥、停止、硬件调用 |

**中央调度是多机器人管理的需要，LLM 是交互和规划增强手段；有多台机器人并不意味着必须有多个大模型互相聊天。**

## 二、真实基础与扩展边界

### 2.1 哪些有依据

| 基础 | 依据 | 可以怎样使用 |
|---|---|---|
| 甘肃 5G 联合创新中心展厅应用、部署验收 | 你提供的简历 | 作为原项目背景，不扩大到新增平台成果 |
| Java 平台及手机人工选机器人、点位、讲解 | 你的业务回忆 | 作为待对接上游；这份 Java 平台源码尚未提供 |
| `G1ControlServer`、导航 Action、控制 Service、状态 Topic | `g1_base` 源码 | 作为真实执行层基础 |
| SDK Python worker、stdin/stdout JSON 通信 | `unitree_sdk_bridge.py` | 讲进程隔离和控制边界 |
| 导航、讲解、停止、状态查询等工具 | `bot_mind/src/mcp/tools` | 作为可适配的机器人能力入口 |
| 展厅地图、点位、讲解文档 | `config/halls.yaml` 和资源目录 | 作为展项台账、资源版本和知识库的基础 |
| 本地 Rust 通信、ZeroClaw 知识目录线索 | Rust 客户端和导入模块 | 证明存在外部智能处理接入点，不证明完整部署拓扑 |
| Java 确定性分配、知识查询工具、闭环模块 | 智能巡检工程 | 借鉴分层与流程，不直接当成机器人平台代码 |

### 2.2 哪些是本次新增设计

中央 Spring AI 编排、多机器人控制权租约、端侧可靠命令收件箱、带会话标识的问答接口、pgvector 知识检索、计划审批版本、故障接替和完整性核对，均按下面的方案设计。

原源码存在对应能力的雏形，但“能调用一次导航”不等于“已经有可恢复的分布式任务系统”。面试中真正值得展开的，正是如何补上这层工程能力。

### 2.3 不应套用的能力

- 不宣称 G1 已支持自主乘电梯、跨楼层、自动换电或自主充电。
- 展项讲解中出现其他品牌机器人能力，不等于执行讲解的 G1 拥有这些能力。
- 不把无人机的三维航迹、Minimum Snap、PPO 和禁飞区直接换名成机器人能力。
- 不宣称机器人自动感知“所有访客已听懂、已跟上”；没有相应感知与验收证据。
- 不声称 Spring AI 会自动提供调度器、物理安全保障或与当前 `/mcp` 的完整兼容。

## 三、课程十三篇内容怎样迁移

来源为你提供的 [HxSay 项目目录](https://www.hxsay.com/blog_page?bucket=hxsay&prefix=blogs%2F%E6%B5%A3%E7%86%8AIOT%E2%80%94%E2%80%94%E5%B7%A5%E4%B8%9A%E6%99%BA%E8%83%BD%E5%B7%A5%E5%8E%82Java%E9%A1%B9%E7%9B%AE%2F)，进入「多级复杂智能巡检中央Agent开发」。以下为逐篇阅读后的迁移结论，不复制课程代码和效果数据。

| 文档 | 借鉴内容 | 展厅场景中的处理 |
|---|---|---|
| LLM 自然语言任务解析&意图识别 | 意图、实体、槽位补全、结果校验 | 提取接待时间、楼层、主题、时长，查台账消歧 |
| 多机复杂智能巡检Agent架构及工作流程 | 中央规划、边缘执行、反馈闭环 | Java 负责业务，bot_mind 负责单机执行，ROS2 负责控制 |
| 任务规划Agent的故障分级处理 | 事件去重、规则定级、差异处置 | 处理导航失败、定位异常、通信丢失和 SDK 异常 |
| 任务规划Agent的智能巡检任务闭环 | 计划与实际核对、缺口补偿 | 核对到点、讲解片段和任务步骤，仅续做未完成部分 |
| 任务规划Agent对于智能巡检的重要性 | 静态路线不能替代动态业务调度 | 地图固定，机器人可用性、接待时段和任务优先级仍变化 |
| 任务规划Agent自动生成巡检工单并提交审核 | 自然语言生成可审核计划 | 生成接待任务单，审批绑定计划版本 |
| 项目亮点二：事件驱动的故障分级与动态复巡 Agent | 事件驱动重规划 | 转为安全暂停、接替建议、人工确认，不照搬危险区复巡 |
| 项目亮点三：Offline-Online 双模自进化的任务闭环 Agent | 在线状态机、离线复盘、版本化 | 离线回放评估后人工发布新策略，不自动改生产安全参数 |
| 项目亮点一：GraphRAG 增强的知识驱动多机任务规划 Agent | 知识、实时资源、可解释分配 | 保留引用和约束；简单展项关系先用关系表 |
| 智能巡检Agent——资源待命与优化 | 就绪池、维护、配置版本 | 任务完成后重新检查健康状态；补电先按人工维护流程 |
| 中央Agent任务分配混合RAG优化方案 | 并行上下文查询、工具封装 | 实时状态与知识分开查询，设置总超时与权限边界 |
| 中央任务规划Agent与多机任务分配 | 能力建模、优先级、分配、下发 | 改为展厅能力约束、业务排队及确定性评分 |
| 浣熊Agent-8.多机复杂智能巡检中央Agent开发-模块索引 | 全模块知识组织 | 作为完整篇目核对入口 |

课程中的百分比、机器人数量、毫秒级优化效果不能迁移为本项目实测结果。部分图形在网页中表现为空白图位或代码文字，本文使用重新组织的文本示意，不声称复现原图。

## 四、整体架构与技术选型

### 4.1 分层关系

```text
工作人员手机 / 原 Java 操作平台
          |
          | 创建、确认、查询、暂停、人工接管
          v
中央调度平台（Java）
  意图解析与规划建议：Spring AI + 经批准的模型服务
  业务执行：任务状态机、确定性调度、审批、审计
  知识问答：RAG；实时状态：受控查询工具
          |
          | 带身份、版本、命令编号的业务协议
          v
各机器人端 bot_mind
  单机任务执行、语音会话、工具适配、事件回传
          |
          | ROS2 Action / Service / Topic
          v
g1_base / G1ControlServer
  导航与避障、动作互斥、FSM 校验、停止优先、状态聚合
          |
          v
SDK worker / Unitree SDK / 机器人硬件
```

两条链路需要分开：接待任务是分钟级长流程；访客问答是绑定某台机器人的短会话。问答可以插入接待，但不能随意更换执行机器人。

机器狗是一条并列的执行分支：`中央平台 → 机器狗任务适配器 → 机器狗端侧控制服务 → 机器狗硬件`。它与 G1 共用业务协议和任务关联标识，不要求共用 SDK、ROS2 服务或导航实现。

### 4.2 一期选型

| 组件 | 用途 | 为什么选、有什么边界 |
|---|---|---|
| Spring Boot + Spring AI | Java 业务与模型接入 | 贴合已有 Java 平台；Spring AI 提供模型和工具接入，不替代业务状态机 |
| MySQL | 任务、审批、控制权、命令及审计 | 本设计的业务库假设，便于与巡检工程思路衔接；原 Java 平台数据库仍待确认 |
| PostgreSQL + pgvector | 知识文本、元数据及向量 | 展厅知识结构相对简单，减少专用向量库和图库的组合复杂度 |
| Redis | 心跳快照、短会话、限流 | 快速读状态；不单独承担任务与控制权的最终事实 |
| HTTP + Outbox + 事件回传 | 指令可靠传送 | 一期规模下易调试；持久化补传，不依靠一次 HTTP 成功 |
| WebSocket | 手机任务进度推送 | 断线重连后按任务快照恢复，不把推送当最终账本 |
| Python FastAPI + ROS2 | 机器人端执行 | 保留现有 bot_mind 和 g1_base 的投入 |
| 文件/对象存储 | 讲稿、地图、动作资源包 | 按版本分发；沿用现有可用设施，不为技术名词新增中间件 |

这里选择两种数据库是“保留 Java 业务生态、隔离知识检索”的折中，不是必要条件。如果原平台本来用 PostgreSQL，可以统一数据库实例、隔离 schema，进一步简化运维。

版本策略：先核对原 Java 平台 JDK、Spring Boot，再锁定兼容的 Spring AI BOM 和 SDK。官方当前文档与旧代码 API 可能不同，本文不把某个最新版号当成原项目已使用版本。

Spring AI 的 `@Tool` 将应用方法暴露为模型可请求的工具，实际执行仍由应用负责；权限和副作用校验应放在应用侧。[官方工具调用说明](https://docs.spring.io/spring-ai/reference/api/tools.html)

### 4.3 为什么一期不全部采用课程技术栈

**不必先上 Neo4j。** “展厅—展项—点位—讲稿”的关系用表关联即可；只有跨系统复杂关系推理确实成为主要查询负载，才评估图库。

**不必先上 PPO。** 需要训练环境、奖励设计、策略验证和部署证据。当前巡检代码里的 `PpoSimulationModel` 实际计算启发式分数，不是训练出来的 PPO 模型，不能靠类名包装成果。

**不必先上 Kafka 或一堆微服务。** 少量机器人场景优先模块化部署，先解决持久化和恢复；事件量、团队边界和独立伸缩需求出现后再拆分。

**不替换 Nav2。** 中央平台决定谁去哪些业务点；Nav2 处理机器人所在地图上的导航执行。固定路线也可以使用局部避障，不能说只有中央 Agent 才能避障。

### 4.4 Spring AI、ZeroClaw、bot_mind 到底怎么分工

目标方案中：Spring AI 承担中央模型接入和受控工具调用；bot_mind 保留语音、讲解和机器人能力服务；g1_base 保留控制执行。

本地代码有 Rust 客户端和 ZeroClaw 知识目录线索，但没有足够部署配置证明“当时所有智能决策都由 ZeroClaw 完成”。外接 LLM 或 Java 平台参与处理完全可能。

迁移时先查清旧链路，再为每个会话配置唯一的智能处理入口。不能让 ZeroClaw 和新中央平台同时消费同一条 ASR 结果并分别控制机器人。

## 五、一次接待任务的完整流程

### 5.1 解析不是直接执行

输入先变成受限结构，例如：

```json
{
  "intent": "CREATE_RECEPTION",
  "hallId": "gansu_5g_1f",
  "requestedStart": "2026-09-10T15:00:00+08:00",
  "durationMinutes": 15,
  "topics": ["液冷机柜", "具身智能"],
  "preferredRobotId": null
}
```

示例日期是假设输入当天的演示值。生产由服务器提供当前日期、时区及明确的时间解释规则；“下午”“尽快”等歧义不能随便猜。

后端负责查验：

- 展项是否存在于目标展厅，是否开放。
- 同名点位是否需要让用户选择。
- 模板时长加预计路程是否可满足十五分钟；不能满足就提示删减。
- 用户能否调度该展厅机器人。
- 模型输出的 ID 是否来自授权候选集，而不是仅仅符合 JSON 格式。

模型失败可以退回表单和固定模板；规则只解析能确定的指令，不应把解析失败包装成成功。

### 5.2 计划是可验证的数据

计划建议分成固定类型的步骤：

```text
NAVIGATE(集合点)
  → WAIT_FOR_OPERATOR_CONFIRMATION
  → NAVIGATE(展项点位)
  → NARRATE(已审核讲稿版本)
  → QA_WINDOW(有时限，可由工作人员结束)
  → NAVIGATE(下一展项)
  → NARRATE(已审核讲稿版本)
  → FINISH
```

模型只能提出白名单步骤，不能生成任意 Python、Shell、关节角、速度控制代码或外部 URL。

计划保存 `planVersion、planHash、hallId、mapVersion、resourceVersion、policyVersion`。审批通过后不覆盖原版本；改变点位、动作、机器人适用范围或讲稿实质内容，需要重新生成并按规则重新确认。

工作人员可以批准“满足指定能力的任意机器人”，也可以锁定某台机器人。前一种允许执行前换候选，后一种不能悄悄替换。

### 5.3 审批和执行之间可能隔了很久

预约时的“机器人空闲”只是预估。到执行时间要重新检查在线性、定位、电量、资源版本和控制权。

如果没有满足条件的机器人，任务进入等待资源或人工处理，手机显示原因。不能为了保证“自动化成功率”忽略条件。

### 5.4 人工操作保留

手机原有“选机器人、选点位、开始讲解”仍然存在。它跳过自然语言解析，但进入同一个任务服务和控制权检查，避免出现“人工接口一套锁，Agent 接口另一套锁”。

## 六、多机器人调度与控制权

### 6.1 先排除不合格，再比较谁更适合

硬条件不满足就不进入评分：

1. 心跳新鲜，控制服务健康，无未消除的故障和停止锁存。
2. 当前楼层和地图版本匹配，定位状态有效。
3. 支持所需导航、播报及审核通过的动作资源。
4. 电量足够覆盖预计任务和保守余量；未知电量不当成满电。
5. 无其他会话或任务的排他控制权。
6. 工作人员有该机器人和展厅的操作权限。

位置缺失不能当作距离为零；状态过期不能因为 Redis Key 还存在就视为在线。

一期采用优先级队列加确定性加权评分，例如：

```text
cost = 0.6 × 归一化预计到达时间
     + 0.2 × 归一化预计能耗成本
     + 0.2 × 归一化近期工作负担
```

权重仅是说明公式的初始假设，需要回放和实际测量。成本越小越优先；相同时按稳定规则选择，保证结果可复现。

先按任务优先级、预约时间及等待时间排序，再为任务选机器人。“同一任务给所有机器人都减一个相同优先级分”并不能改变机器人之间的排名。

这是可解释的启发式调度，不声称全局最优。复杂的多任务、时间窗问题出现后，可以评估最小费用流或约束求解器；它们也不意味着必须采用大模型求解。

### 6.2 具体例子

| 机器人 | 当前情况 | 决策 |
|---|---|---|
| G1-A | 一楼、空闲、状态有效、资源匹配 | 进入评分 |
| G1-B | 一楼、正在为另一组访客讲解 | 排除，不能为了距离近抢走 |
| G1-C | 二楼、空闲 | 排除，不假设它能自行下楼 |

以上是多机器人设计用例，不是实际部署数量。即使 G1-A 距离稍远，也可能是唯一合格候选。

### 6.3 平台两个实例同时抢一台机器人怎么办

在业务库为每台机器人维护唯一控制权记录：`robotId、ownerTaskId、leaseEpoch、expiresAt、version、state`。

平台事务通过行锁或条件更新竞争控制权，只有一个成功。成功后在同一事务中落地任务分配和 Outbox 记录。Redis 可以加速竞争，但最终约束依赖数据库。

`leaseEpoch` 是单调增加的控制代次。端侧持久化已接受的代次，拒绝旧控制者迟到的命令；新代次也不能自动清除正在运行的动作，必须完成接管握手和停机核对。

特别注意：**租约过期只表示原控制者失去授权，不表示机器人已经停止。** 若机器人还在执行或状态未知，应隔离为待核对，不能直接释放给另一任务。

端侧进程重启后先恢复命令账本、检查物理状态并重新握手；不直接从“内存为空”推断为可调度。

### 6.4 中央不负责两台设备如何绕开彼此

导航路径、动态避障和停让属于端侧控制能力，G1 由 g1_base 负责。中央不计算两台设备的避碰轨迹，也不保证仅靠业务排队就能消除物理冲突。

中央可以预约互动展位的使用时间，避免两个表演任务占用同一场地；这是业务资源预约，不是交通控制。端侧报告无法到位时，中央等待、改期或请工作人员处理。多设备同场运行的实际安全性需要端侧能力和现场验证支持。

### 6.5 G1 与机器狗协同：一个主任务，多条依赖子任务

不能再把整个接待简单建模为 `taskId → 一个 robotId`。应建模为接待主任务下的角色与子任务：

| 角色 | 分配对象 | 子任务 |
|---|---|---|
| GUIDE | 支持导航和讲解的 G1 | 带队、展项讲解、互动口令、互动后续讲 |
| PERFORMER | 已验证支持拜年技能的机器狗 | 在指定展示位置准备、等待互动触发、执行拜年 |

机器人能否移动到互动位由其端侧能力决定；如果机器狗没有接入导航，就由工作人员提前摆放并确认准备完成，不能虚构自动移动能力。

业务依赖可以用有向无环图（DAG）表示，本质是“哪些可以同时做，哪些必须等前一步完成”：

```text
接待主任务
  ├─ G1：带队 → 液冷讲解 → 到具身智能展项 → 互动介绍 ─┐
  └─ 机器狗：准备展示位置 → 技能及状态检查 → READY ────┤
                                                     v
                                      双方就绪 + 互动区域确认
                                                     |
                                      G1 播报“请给大家拜个年”
                                                     |
                                         等待口令播放完成
                                                     |
                                           机器狗执行拜年
                                                     |
                                         等待动作完成回执
                                                     |
                                      G1 接话、继续讲解/问答
```

这里没有必要让两个大模型自行商量。LLM 将需求映射到审核过的“双机互动模板”，程序展开角色、步骤和依赖；每台设备的动作仍由各自执行层完成。

### 6.6 “出声命令机器狗”如何落地

这句话有两种技术含义，不能混为一谈：

**本设计默认：语音展示，结构化指令执行。** G1 确实出声说出命令；中央收到该口令的播放完成事件后，向绑定的机器狗发送白名单技能命令。观众看到的是 G1 发出口令、机器狗响应，实际动作触发由可靠协议保证，不依赖展厅收音效果。

**如果要求机器狗真正听懂口令：** 必须额外具备机器狗拾音、ASR 和口令识别能力。限定互动会话、唤醒窗口和白名单，识别成功后仍经端侧状态校验执行。环境噪声、其他机器人声音和重复识别都要测试；目前不能认定已提供的源码包含这条链路。

两种模式只能指定一个权威触发来源，不能语音识别触发一次、中央再触发一次。默认模式也不能在面试中说成“机器狗独立理解了 G1 的自然语言”。

### 6.7 多设备资源预约、时长和失败处理

计划阶段检查十五分钟预算是否覆盖带队、两段讲解、互动准备、口令和动作；预计时长由模板、端侧估计或历史测量提供，不由模型随口给出。机器狗准备可以与液冷讲解并行，但互动节点必须等待双方就绪。

执行前取得所需角色的有效资源预约，避免 G1 已接待、机器狗却被其他表演占用。数据库中的多设备占用按稳定顺序在短事务内获取，竞争失败回滚本次占用，不拿着一台设备无限等另一台。远程就绪不在数据库事务中等待；预约也不替代互动开始前再次检查。

机器狗可以只预约互动时间窗，但预约窗口需要保守余量。若前序讲解拖延，重新确认后续预约，不能占用其他接待的时间。

互动使用唯一 `interactionId`，关联双方子任务、口令命令与动作命令。只有当前互动的正确口令完成事件才能触发拜年；暂停、取消后迟到回执不能再次推进。

- 机器狗未就绪：在预算内有限等待，超时告知工作人员。
- 口令播放失败：不触发拜年，进入重试或人工处理。
- 拜年结果未知：查询并确认，不重复表演、不直接宣布成功。
- 机器狗故障：暂停互动，由工作人员决定跳过或终止；是否可跳过应在计划中标注。
- 必选互动未完成：主任务不能标成完整成功；可选互动跳过也要在结果中明确记录。
- 人工停止：取消双方未启动步骤，向受影响设备发送停止/取消请求，等待各自端侧反馈。

### 6.8 协同模型的代码与数据增量

第十一、十二章的基础结构需要按以下规则扩展，避免仍用单机器人字段表达多机任务：

- `reception_task` 保存接待主任务；原 robotId 仅可表示主讲机器人，不能代表全部参与设备。
- 新增 `task_participant`：taskId、role、robotId、预约时间窗；记录 GUIDE 与 PERFORMER 的绑定。
- `reception_step` 增加 participantId、dependsOn、interactionId、required、timeoutPolicy；每个执行步骤有明确归属。
- 新增 `interaction_run`：interactionId、taskId、双方参与者、状态、版本；表达 PREPARING、READY、CUE_PLAYING、PERFORMING、COMPLETED、FAILED、CANCELED。
- `execution_command` 继续携带具体 robotId，并增加 interactionId/participantId；能力命令使用审核过的 skillCode 和 skillVersion，不允许生成任意机器狗动作参数。
- 新增 `InteractionCoordinator` 管理依赖屏障，`RobotGroupReservationService` 管理组合预约，`DogTaskAdapter` 对接机器狗端侧。具体机器狗 SDK/API 未提供，适配器是待实现契约。

中央收到双方 READY 并不立即表演：还要确认预约有效、业务未暂停、端侧安全状态允许。最终能否执行拜年，仍由机器狗自己的控制层校验。

## 七、任务状态与可靠执行

### 7.1 三层状态，不能混用

| 层级 | 例子 | 完成条件 |
|---|---|---|
| 接待任务 | 接待这一组访客 | 必需步骤完成，或人工明确接受部分完成并单独标记 |
| 任务步骤 | 到液冷展项并讲解 | 导航到点、对应讲稿播放完成等证据齐全 |
| 执行命令 | 执行一次 NAVIGATE | 端侧提供确定的成功/失败/取消结果 |

建议任务主线：

```text
DRAFT → PENDING_APPROVAL → WAITING_RESOURCE → READY → RUNNING
                                                      |
                                 完整性核对通过 → COMPLETED
                                 有缺口 → PARTIAL → 续做/人工处理
                                 需干预 → MANUAL_REQUIRED

RUNNING → PAUSING → PAUSED → 恢复前校验 → RUNNING
RUNNING → CANCELING → 确认停止 → CANCELED
```

命令状态：`PENDING → ACCEPTED → RUNNING → SUCCEEDED / FAILED / CANCELED`。无法判断执行结果时为 `UNKNOWN`，不是成功，也不是可立即重试。

### 7.2 HTTP 200 不代表机器人做完了

必须分清：网络收到请求、端侧接受命令、底层开始执行、底层报告完成、业务核对完成。

当前 `/voice/tts/speak` 启动后台线程就返回“正在播放”，它不是播放完成回执。现有工具返回自然语言成功描述，也不足以证明接待任务完成。

新增端侧执行器应把底层导航结果、TTS 生命周期和中断信息变成明确事件。暂停讲解不是 SUCCEEDED；取消请求发出也不等于 CANCELED。

### 7.3 可靠传递怎么做

1. 中央事务保存任务、命令及 Outbox，提交后再发送。
2. 发送超时重试**同一个 commandId**。
3. 端侧先按 commandId 落地收件箱，重复请求返回已有状态，不重复执行。
4. 执行事件持久化并补传；中央按 eventId 去重、按命令和版本更新。
5. 收不到结果，主动查询命令状态；无法确认则进入 UNKNOWN。

Outbox 解决“数据库提交了但请求没发出”；收件箱解决“网络重试导致重复启动”。两者都不能保证机器人物理世界的绝对 exactly-once。

如果机器人刚移动完就断电，完成事件尚未保存，恢复时可能不知道命令到底完成没有。此时应重新定位、核对目标和执行状态，必要时人工确认，而不是盲目重放动作。

### 7.4 重试与补偿

只读查询可以有限重试。导航失败要先确认旧目标已取消且机器人稳定，再决定是否重试；新业务尝试使用新 attempt 和 commandId，并关联旧尝试。

动作和讲解不能一律从头重来。讲解按已确认的段落边界保存检查点，恢复时允许重读一个短句，不承诺音频采样级断点续播。

接替机器人仅承接未完成步骤，并确认访客和工作人员接受接替。补偿是续做、改期、人工接管等业务动作，不是“回滚已经走过的路”。

归档失败只重试归档，不能重新启动一次机器人接待。

## 八、上下游接口与协议

### 8.1 现有接口与新增接口分开

| 层 | 现有可见能力 | 新增设计 |
|---|---|---|
| bot_mind | `/mcp` JSON-RPC，工具枚举与调用 | 统一鉴权、可靠命令、标准化结果 |
| bot_mind 语音 | `/voice/tts/speak` 等接口 | robot/session/turn/epoch 标识，播放生命周期 |
| g1_base | 导航 Action、控制 Service、状态 Topic | 保留能力，向上适配更明确的任务事件 |
| 原 Java 平台 | 由你描述的人工操作能力 | 原 API 未提供，不虚构已存在路径 |

下表路径全部是新平台建议契约：

| 调用方向 | 接口 | 语义 |
|---|---|---|
| 手机 → 中央 | `POST /api/reception/plans` | 表单/自然语言生成草案，返回 planId |
| 手机 → 中央 | `POST /api/reception/plans/{id}/approve` | 携带 planVersion，审批指定版本 |
| 手机 → 中央 | `GET /api/reception/tasks/{id}` | 查询权威快照 |
| 手机 → 中央 | `POST /api/reception/tasks/{id}/pause` | 申请暂停，结果异步确认 |
| 手机 → 中央 | `POST /api/robots/{id}/takeover` | 有权限的人工接管并核对旧任务 |
| 中央 → 端侧 | `POST /edge/v1/commands` | 快速接受，202 不等于执行完成 |
| 中央 → 端侧 | `GET /edge/v1/commands/{id}` | 查询命令状态 |
| 中央 → 端侧 | `POST /edge/v1/commands/{id}/cancel` | 请求取消，等待实际结果 |
| 端侧 → 中央 | `POST /api/edge/events` | 可靠上报执行事件，可批量补传 |
| 端侧 → 中央 | `POST /api/edge/heartbeats` | 上报状态快照，不用每条轨迹落业务表 |
| 端侧 → 中央 | `POST /api/dialogue/turns` | 提交指定机器人会话的 ASR 文本 |
| 中央 → 端侧 | `POST /edge/v1/dialogue/replies` | 下发带代次的纯文本回答 |

### 8.2 一条导航命令应该包含什么

```json
{
  "commandId": "cmd-demo-001",
  "taskId": "task-demo-001",
  "stepId": "step-demo-002",
  "attempt": 1,
  "robotId": "G1-A",
  "leaseEpoch": 42,
  "expectedBootId": "boot-demo-a",
  "planVersion": 3,
  "hallId": "gansu_5g_1f",
  "mapVersion": "map-demo-v3",
  "resourceVersion": "bundle-demo-v5",
  "type": "NAVIGATE",
  "target": {"waypointId": "wp-demo-liquid-cooling"},
  "startDeadline": "2026-09-10T15:00:10+08:00",
  "executionTimeoutMs": 120000
}
```

所有 demo 值只是契约示意，不能直接用于设备。`startDeadline` 防止迟到命令启动；执行超时控制运行时长，两者不同。

端侧按认证身份确认 robotId，不能相信调用方随意填的正文。点位 ID 在匹配版本资源中解析成真实坐标，模型不直接给坐标。

### 8.3 事件示例

```json
{
  "eventId": "event-demo-017",
  "robotId": "G1-A",
  "bootId": "boot-demo-a",
  "commandId": "cmd-demo-001",
  "attempt": 1,
  "sequence": 7,
  "state": "SUCCEEDED",
  "resultCode": "GOAL_REACHED",
  "occurredAt": "2026-09-10T15:01:00+08:00"
}
```

中央以 eventId 唯一约束去重；sequence 按单命令流递增，旧事件不能覆盖新状态。不同 bootId 的事件不能只比序号大小，必须结合命令归属和恢复规则核对。终态更正通过专门的对账记录处理，不能任意倒退。

### 8.4 MCP 该放在哪里

MCP 是工具接入协议，不是机器人长任务可靠执行协议。模型侧工具可以调用“创建计划、查询状态、检索知识”；正式执行由审批后的任务引擎发命令。

当前 `http_transport.py` 有 `initialize / tools/list / tools/call`，但协议版本字段及自定义结果封装需要核对，不能因路径叫 `/mcp` 就认定兼容标准客户端。

一期可以用 Java `@Tool` 包装内部服务，通过 `LegacyBotMindAdapter` 适配现有 JSON-RPC；标准化后再接 Spring AI MCP 客户端。验收需要验证初始化协商、工具 schema、调用结果、错误与超时，而不只是测试 tools/list。[Spring AI MCP 客户端文档](https://docs.spring.io/spring-ai/reference/api/mcp/mcp-client-boot-starter-docs.html)

## 九、语音问答与中断续讲

### 9.1 当前 bot_mind 是干什么的

它不是一个简单的“LLM 转发器”。它组合了语音输入输出、对话状态协调、展项讲解、导航工具和 ROS2 服务调用，是单台机器人的业务执行与交互层。

可见链路包含：麦克风 ASR 结果进入语音引擎，再经 Rust 客户端发送到配置端口。外部如何调用模型、是否经 Java 平台、知识如何索引，仅凭这个客户端不能完整还原。

导入模块只替换 ZeroClaw 的知识文件目录，文档明确说不触发索引导入命令，也不等待向量库结果。因此“文件复制完成”不等于“RAG 已经可检索”。

### 9.2 新方案如何避免串台

每条问答绑定：`robotId + sessionId + turnId + generationEpoch`。

- robotId：回答只能在对应机器人播放。
- sessionId：区分不同访客会话；会话结束清理上下文。
- turnId：区分每一轮问题及 ASR 重复上报。
- generationEpoch：打断、切换任务或接管后递增，拒绝旧回答。

访客可以问“液冷如何节能”，但不因此获得管理员派单权。语音设备身份也不能等同于当前说话人的员工身份。

### 9.3 讲解过程中有人提问

```text
讲解播放中
  → 唤醒/打断
  → 停止当前播报，保存讲解段落检查点
  → 创建问答 turn，检索并生成回答
  → 端侧检查会话和 generationEpoch
  → 播放纯文本回答
  → 按规则或人工确认恢复讲解
```

打断时尽力取消模型请求和 TTS，同时必须丢弃旧代次结果，因为网络取消不保证远端立即停止计算。

现有接口只有 text，无法完整识别迟到回答；仅判断“当前是否 ASR 中”也不够。需要新增会话字段，而不是在 Prompt 里要求“不要串台”。

### 9.4 固定讲解和自由问答为什么分开

固定讲解使用审核过的讲稿和动作资源，强调内容稳定。自由问答从授权知识中生成回答，强调引用和不确定性处理。

源码讲稿中包含 VHML 动作标签。知识入库应剥离执行标签，问答输出只能进入纯文本播放通道；不能让检索文本或模型生成的标签触发机器人动作。

对于可信讲解动作，也应通过白名单资源标识执行，不能把任意模型字符串解释成脚本。

## 十、RAG知识库设计

### 10.1 三类知识，两类使用者

| 知识 | 例子 | 使用者 |
|---|---|---|
| 展厅公开知识 | 液冷机柜、具身智能等展项介绍 | 访客问答 |
| 机器人操作知识 | 如何切换展厅、定位初始化注意事项 | 授权工作人员 |
| 故障处理知识 | 某错误码对应的检查步骤 | 运维人员 |

G1 的电量、正在执行哪个任务是实时状态，应查接口；不能用向量数据库里昨天的文本回答。

### 10.2 知识从哪里来

第一批可以使用现有讲稿作为经过清洗的候选语料，再补充经审核的 G1 操作手册与现场故障记录。不能假设当前仓库已包含完整运维知识。

每个分块保存：`docId、docVersion、chunkId、hallId、boothId、robotModel、visibility、source、section、embeddingModelVersion`。

“具身智能”展项可能介绍其他机器人的换电技术，因此回答“你自己能换电吗”时必须区分展品介绍和当前机器人能力。语义相似不等于对象相同。

### 10.3 入库流程

```text
上传/同步 → 内容审核 → 清洗执行标签与敏感信息
        → 按标题和主题切块 → 生成向量 → 写入新版本
        → 检索检查 → 发布版本 → 新请求使用新版本
```

按段落和语义切块，保留标题与必要上下文，不机械切断操作步骤、表格或警告。块长和重叠根据模型窗口与评测调整，不把某个固定长度当通用最优值。

入库有 jobId 和分块幂等键；失败重试不重复生成整套有效版本。文档版本未完整就绪不切换；删除文档同时撤销可检索状态，后台再清理孤儿块。

向量库与业务库不做假想的跨库原子事务：使用入库任务状态、版本发布和补偿对账。知识内容与索引发布状态尽量放同一 PostgreSQL 事务边界内。

### 10.4 pgvector 怎么实现混合检索

本方案的“混合”指两个召回通道：

1. pgvector 语义召回，处理“液冷为什么省电”这类表达变化。
2. 中文分词后的关键词/全文召回及展项别名精确匹配，处理产品名、缩写和故障码。

结果按 chunkId 去重，用自实现 RRF 按排名融合，再按评测决定是否增加 reranker。PostgreSQL 默认全文检索配置不能直接当成完善的中文分词方案；需选定分词预处理并验证。

pgvector 支持精确近邻和 HNSW/IVFFlat 等近似索引。小知识库先用简单方案测基线；增加索引后检查带过滤条件时的召回，不只测无过滤速度。它也不是自带完整 BM25 和问答流水线的产品。[pgvector 官方说明](https://github.com/pgvector/pgvector)

当前巡检工程的 `HybridRagService` 主要是 Milvus 语义结果与 Neo4j 图结果聚合，不等于已经实现这里的关键词、向量、RRF 与重排。

### 10.5 RAG 能不能控制机器人

RAG 可以解释“为什么当前不能安排这台机器人”，提供操作步骤参考，也可以帮助工作人员选择讲解主题。

但生产硬约束来自审核发布的结构化策略和实时状态，不在每次调度时从任意文本里临时抽取。例如禁止跨楼层调度应是规则，不应依赖向量检索刚好召回某一段说明。

文档更新产生新的规则建议，必须经过审核后发布为策略版本。不能靠“检索到了禁用关键词”就稳定建立安全系统。

### 10.6 权限、引用和拒答

权限过滤由服务端身份生成，对所有召回通道一致生效；没有命中时不能把范围扩大到全库绕过权限。公开知识回退也必须明确属于公开集合。

回答保存引用块及文档版本；没有证据的型号参数不编造，建议工作人员确认。实时故障先展示确定的错误码和状态，LLM 只能补充知识解释，不能降低安全级别。

评测分别看检索召回、回答有据率、错误拒答、权限泄漏、型号混淆和耗时。多加一个数据库并不自然提高这些指标。

## 十一、项目结构与核心类

以下是建议结构，不是声称这些文件已经存在。优先模块化单体部署，AI 调用采用独立线程池和限流，后续再按负载拆服务。

```text
embodied-central-platform/
  platform-app/                 启动、装配、配置
  platform-reception/
    api/                       ReceptionPlanController
    application/               ReceptionApplicationService
    domain/                    ReceptionTask、TaskStep、PlanSnapshot
  platform-agent/
    planning/                  ReceptionPlanningAgent、IntentResolver
    tools/                     HallQueryTools、RobotQueryTools
    policy/                    PlanValidator、ToolAccessPolicy
  platform-scheduling/
    application/               SchedulingService
    policy/                    EligibilityPolicy、CandidateScorer
    ownership/                 RobotLeaseService
  platform-execution/
    workflow/                  TaskStateMachine、StepExecutor
    delivery/                  CommandOutboxWorker
    events/                    EdgeEventConsumer、ReconciliationJob
    adapter/                   RobotGateway、LegacyBotMindAdapter
  platform-dialogue/
    application/               DialogueTurnService
    session/                   DialogueSessionStore
  platform-knowledge/
    ingest/                    KnowledgeIngestService
    retrieval/                 HybridRetriever、RrfFusion
    answer/                    EvidenceAnswerService
  platform-infrastructure/     DB、Redis、HTTP、认证、审计
```

机器人端增量设计：

```text
bot_mind/src/
  edge_protocol/               新增：可靠命令和事件协议
  execution/                  新增：收件箱、状态机、检查点
  mcp/                        复用：现有工具与传输适配
  service/                    复用：导航、语音、资源服务
  voice/                      复用并增强：会话代次和中断

g1_base/g1_base/
  g1_control_server.py        现有：控制入口
  unitree_sdk_bridge.py       现有：SDK 子进程桥接
  nav_core.py                 现有：导航控制基础
```

现有 `g1_control_client.py` 位于 `bot_mind/src/service`，由它对接 g1_base，不在两个工程里重复创建控制入口。导航进度需要将底层反馈完整接到平台；不能因为 Action 服务端发布了反馈，就假设当前客户端和平台已经消费了反馈。

### 核心调用链

```text
ReceptionPlanController
  → ReceptionApplicationService
  → ReceptionPlanningAgent / 表单模板入口
  → PlanValidator
  → 保存草案与审批

执行时：SchedulingService
  → EligibilityPolicy → CandidateScorer
  → RobotLeaseService
  → TaskStateMachine + Outbox（同一事务）
  → RobotGateway → 端侧 StepExecutor → 现有机器人服务

结果：EdgeEventConsumer
  → 去重与状态迁移
  → 完整性核对
  → 下一步骤 / 续做 / 人工处理 / 完成
```

不要在 Controller 中串起一个持续十五分钟的阻塞调用；接口只接受业务请求并返回任务编号。AI 解释失败不回滚已经提交的执行命令。

## 十二、数据库与缓存

这是业务模型建议，不要求为面试记住所有字段。用户、角色、权限和 SSO 映射复用原平台，下面不重复计算。

| 表/模型 | 关键字段 | 主要约束 |
|---|---|---|
| `robot_registry` | robotId、model、endpointRef、capabilities、enabled | robotId 唯一；地址由平台配置，不由模型提供 |
| `hall_waypoint` | hallId、mapVersion、waypointId、pose、enabled | 展厅+版本+点位唯一 |
| `resource_release` | hallId、version、hash、manifest、status | 发布版本不可随意覆盖 |
| `reception_task` | taskId、planVersion、status、robotId、parentTaskId、version | 状态条件更新，父子关系可追溯 |
| `reception_plan_version` | taskId、version、planHash、snapshot | 同任务版本唯一 |
| `reception_step` | stepId、taskId、sequence、type、status、checkpoint | 同任务步骤顺序唯一 |
| `task_approval` | taskId、planVersion、approverId、decision、time | 审核目标明确，不审核“最新的某版” |
| `robot_control_lease` | robotId、ownerTaskId、epoch、expiresAt、state | 每台机器人一条权威控制记录 |
| `execution_command` | commandId、stepId、attempt、payloadHash、state | commandId 唯一，重复 ID 不允许不同正文 |
| `message_outbox` | messageId、commandId、nextRetryAt、status | 投递可重试，不在事务内访问机器人 |
| `execution_event` | eventId、commandId、bootId、sequence、payload | eventId 唯一，完整审计 |
| `decision_audit` | taskId、candidateSnapshot、scores、reasons、versions | 记录淘汰原因，不只存 LLM 解释 |
| `policy_version` | version、content、approvedBy、effectiveAt | 已运行任务固定策略版本 |

知识库另有 `knowledge_document、knowledge_chunk、ingest_job` 等模型；端侧可用 SQLite 保存命令收件箱与事件待发送记录。会话内容是否长期入库按隐私和运维需求决定，不默认保存原始录音。

Redis 示例：机器人最新状态、会话短历史、限流计数。心跳必须有采集时间和接收时间，不能只看 TTL。数据库旧快照可以用于页面展示，不能不检查新鲜度就作为调度兜底。

重点索引包括任务 `(status, scheduledTime)`、Outbox `(status, nextRetryAt)`、事件 `(commandId, sequence)`。实时位置不要每次都更新整张任务主表。

## 十三、安全与异常处理

### 13.1 身份分成三种

- 工作人员：SSO/本地登录态与业务权限，控制哪些展厅和操作。
- 机器人：独立服务身份，可采用部署条件允许的 mTLS 或短期服务凭据，只上报自身数据。
- 访客：只允许公开问答和明确允许的交互，不获得管理权限。

内部网络不等于可信。当前机器人路由的认证覆盖需要补齐，不能把控制接口直接开放到公网。密钥不写进模型上下文、日志或文档。

### 13.2 异常矩阵

| 异常 | 处理 | 不能做什么 |
|---|---|---|
| LLM 超时 | 退回模板/表单，已有任务继续 | 阻塞停止控制 |
| RAG 不可用 | 固定讲解可用则继续，问答明确暂不可回答 | 编造资料 |
| 心跳超时 | 禁止新派单，任务标记待核对 | 认定机器人已停止 |
| 导航超时 | 请求取消、核对稳定状态，有限恢复 | 无限重复发导航 |
| 定位失效/地图不匹配 | 阻止移动，提示现场处理 | 以旧坐标强行导航 |
| SDK worker 阻塞 | 本地停止通道及硬件措施，隔离故障 | 只重启进程就宣称物理安全 |
| 电量不足 | 暂停派单，人工补电或确认接替 | 假设自动回充已经可用 |
| 回调重复乱序 | 去重、单调状态迁移、对账 | 让旧 RUNNING 覆盖 SUCCEEDED |
| 讲解被打断 | 保存检查点，进入问答或暂停 | 把中断记作完整讲解 |
| 任务结束但归档失败 | 单独重试归档 | 重跑导航和讲解 |
| 火警等现场安全事件 | 执行批准的停用/疏散人工流程 | 自动派 G1 前往危险区调查 |

### 13.3 停止优先不靠大模型

停止请求走受控的直接通道，绕开 LLM、普通任务队列和长时间占用的执行锁。端侧取消导航、停止播报和脚本，并保持停止锁存，避免旧速度命令恢复运动。

现有 g1_base 已有停止优先和锁存相关实现，可以讲真实思路。但软件停止不是经过认证的硬件急停；SDK 阻塞、网络断开时必须依赖本地安全设计及现场操作规程。

### 13.4 断网不等于全面离线自治

一期保守策略：失联后不启动新步骤，当前步骤根据风险和端侧状态安全结束或停止，事件本地缓存。恢复连接后先对账，再由人员或批准策略恢复任务。

若 ASR、LLM、TTS 本来依赖网络，断网时不能宣称自由问答仍正常。可预缓存固定讲解音频，但这是明确建设的降级能力，不是缓存文本自然就有的能力。

## 十四、部署测试与迭代

### 14.1 部署方式

中央 Java 服务运行在展厅内网或批准的专有环境，机器人通过受控网络接入。模型调用可接企业允许的推理服务；不假设全部访客语音可以发到公网模型。

每台机器人运行独立 bot_mind 和 g1_base。现有 ROS2 使用绝对服务名，多机器人部署还要隔离 DDS Domain/网络，或系统性处理 namespace 和 TF frame；仅在 HTTP 正文加 robotId 不够。

平台可以做双实例，通过数据库控制权竞争避免双派单。停机后重启从持久化任务恢复，但先查询端侧状态，不重放所有 RUNNING 命令。

### 14.2 应测什么，而不是编一组数字

| 测试 | 验收关注点 |
|---|---|
| 相同命令重复投递 | 只创建一条端侧执行记录，不重复启动 |
| 执行后回执丢失 | 查询恢复，不立即重新移动 |
| 两个平台实例抢机器人 | 仅一个取得有效控制权 |
| 旧租约命令迟到 | 端侧拒绝旧代次 |
| 一问未答又被打断 | 旧答案不播放、不执行动作 |
| 两机器人同时问答 | 会话、语音、工具目标不串台 |
| 讲稿更新 | 运行任务不混用版本，新任务可用新版本 |
| 私有手册检索 | 访客召回和回答中均不可泄露 |
| 错误地图和跨楼层请求 | 前后端与端侧均按职责拦截 |
| 中央进程崩溃/机器人重启 | UNKNOWN 对账，不假装恢复成功 |
| 停止与脚本同时到达 | 停止优先，旧动作不复活 |

先用 2～5 个模拟机器人作为测试夹具，再在受控场地做单机联调与多机验证。这不是实际机器人数量或已经完成的测试结果。

### 14.3 性能与成本怎么说

分开测自然语言解析耗时、纯调度耗时、命令接受耗时、导航执行耗时和问答首音延迟；不能把数据库查询的 200ms 当作整个 Agent 到机器人行动的耗时。

容量估算示例：20 个模拟机器人按 1Hz 上报就是每秒 20 条心跳，不代表系统仅凭这个数量就完成容量验证。仍需同时测试问答并发、模型限流、数据库写入、事件补传和故障恢复。

运行成本主要来自推理、语音服务、服务器、数据库与运维。通过固定讲稿、限制上下文、工具轮数上限、分离 AI 线程池降低成本；不是让每个心跳调用一次大模型。

### 14.4 迭代顺序

1. **接口摸底**：获取原 Java 平台接口、端侧部署和脱敏模型配置，确认单一控制入口。
2. **执行闭环**：先实现表单任务、可靠命令、状态核对与人工接管。
3. **多机调度**：加入能力过滤、资源占用、版本与冲突处理。
4. **智能入口**：自然语言草案、审批、解释、超时降级。
5. **知识问答**：公开讲稿 RAG、权限隔离、会话打断、评测。
6. **持续优化**：从日志回放改评分与提示词，版本化发布。

课程的 Offline 优化是离线分析/回放，不等于网络断开。本文不设计自动训练后直接修改机器人安全策略。策略变更先离线评测、影子运行、人工确认，再让新任务使用。

## 十五、项目介绍与职责表达

### 15.1 九十秒介绍

> 原项目面向甘肃 5G 联合创新中心的展厅讲解和接待，已有手机端选机器人、点位和开始讲解的 Java 平台。我负责的机器人侧基础包括 G1ControlServer、ROS2 导航与控制接口、SDK 子进程隔离、动作编排及控制互斥。
>
> 在这套基础上，我进一步设计了中央调度扩展：使用 Spring AI 理解接待意图并生成可审核计划，确定性调度器按楼层、地图、能力、状态和控制权选择机器人；bot_mind 保留单机交互和执行能力，g1_base 保留本地运动与安全控制。
>
> 方案重点不是让模型直接控制电机，而是补齐任务下发后的工程闭环，包括命令幂等、状态核对、打断续讲、会话隔离和人工接管。知识问答采用面向展厅资料的 RAG，公开讲解与内部操作手册隔离，实时状态则通过接口查询。

其中“设计”不能未经验证改成“已上线”。如果后续完成实现与验收，再据实更新动词和结果。

### 15.2 最适合突出你的三项工作

**机器人执行抽象。** 从真实 G1 控制经验出发，解释长导航为什么用 Action、短控制为什么用 Service、状态为什么用 Topic，以及这些信号如何映射成平台任务事件。

**可靠任务协议。** 解释为什么一次工具成功不等于业务完成，如何处理超时、重复、取消、掉线和接替。这个方向与后端工程面试高度相关。

**知识与控制边界。** 展项问答使用 RAG，任务动作使用审核过的类型和资源；模型可以建议但不能越过控制权、安全和权限校验。

不建议把自己的职责扩成“独立实现全部模型训练、前端、机器人导航算法、平台、国产化适配和部署验收”。面试官通常会沿你声称最熟悉的一层追问。

## 十六、面试题与参考回答

以下回答采用本方案的目标设计口径。讲已完成经历时只使用有实际依据的部分；“设计了”和“实现并验证了”是不同承诺。

### A. 背景与选型

#### 1. 原平台已经能点按钮了，为什么还做中央 Agent？

按钮可以完成单项操作，但接待涉及多个步骤、时间和机器人资源。中央平台把任务、资源、执行结果统一管理，Agent 再把自然语言变成可审核计划。人工按钮仍保留，并且走同一条执行链路。

#### 2. 多机器人一定要多 Agent 吗？

不一定。多机器人需要中央资源协调，但执行器可以是普通状态机。这里的规划、知识、执行是职责分层，不要求每层都有独立大模型；一期可以只有一个受控规划入口和一个知识问答入口。

#### 3. 为什么选择 Spring AI？它能替代 ZeroClaw 吗？

已有上游是 Java 平台，Spring AI 便于在 Java 服务里统一模型、工具和知识调用。它可以承接相应智能编排职责，但不是安装后就替代所有运行时功能。需要先核实旧链路，把会话、知识和工具迁移完整，保留 bot_mind 的机器人业务服务。

#### 4. 为什么不直接把导航工具给 LLM？

因为导航有物理副作用，还涉及权限、机器人占用和地图版本。模型生成计划建议，审批和程序校验后由任务引擎下发；只用 Prompt 限制不能防止错误参数或重复调用。

#### 5. 你们用了强化学习调度吗？

本方案一期不用。采用硬约束过滤、优先级队列和可解释评分。参考巡检源码中的 PPO 命名类实际是启发式实现，不构成强化学习成果。只有明确优化目标、训练环境和评测收益后才考虑引入。

#### 6. 为什么 RAG 选 pgvector 而不是 Milvus 加 Neo4j？

展厅资料主要是讲稿和手册，关系结构也较简单。pgvector 能与文档元数据一起维护，便于构建规模适当的系统。Milvus 和图数据库不是不好，而是要用数据规模、查询类型和运维成本证明新增组件值得。

### B. 规划与调度

#### 7. 怎么保证模型没有编造展项和点位？

解析只产生候选结构，后端根据真实台账和授权范围解析 ID，验证楼层、版本和开放状态。同名或缺失字段就追问；符合 JSON schema 只能说明格式对，不能证明业务正确。

#### 8. 怎么挑一台机器人？

先排除状态过期、电量不足、地图不匹配、能力不足和被占用的机器人，再根据预计到达时间、能耗和近期负担评分。保存候选快照及淘汰原因，便于解释和复盘。

#### 9. 为什么不用离任务最近的机器人？

最近的可能在接待其他访客，或者楼层、地图不匹配。距离是软指标，能力和安全是硬条件。先满足硬条件再优化成本。

#### 10. 审批时有空，执行时机器人忙了怎么办？

审批绑定计划和允许的资源范围，不永久锁住预约机器人。执行前重新获取状态并占用资源；若审批允许同能力替换，就在范围内重新选择，否则通知工作人员确认或改期。

#### 11. 两个请求同时分配同一台机器人怎么办？

通过数据库中的唯一控制权记录和条件更新竞争，事务内同步保存任务及待发送消息。端侧还验证控制代次，防止旧平台实例的迟到命令执行。

#### 12. Redis 分布式锁就够了吗？

不够。锁到期不代表机器人停止，平台暂停也可能导致旧请求迟到。还需要持久化控制权、端侧代次校验、停止核对及恢复握手；Redis 只解决部分协调问题。

#### 13. 紧急任务怎么插队？

先区分业务高优先级和安全停止。业务任务只能按规则暂停并确认旧任务稳定后接管，不能突然抢走正在带队的机器人；安全停止走直接通道，不等待普通排队或 LLM。

### C. 执行与可靠性

#### 14. 导航为什么用 ROS2 Action？

导航是长时间任务，需要进度、结果和取消。Action 能表达这些过程；短控制适合 Service，持续状态适合 Topic。平台再把这些信号转换成业务命令状态，不能把 Service 应答当作所有动作都完成。

#### 15. SDK 为什么放子进程？

隔离 SDK 初始化、运行环境和异常影响，主 ROS 进程通过带请求编号的 JSON 通信调用 worker。它降低耦合，但并不自动消除所有阻塞，还需要超时、退出检测和独立安全停止措施。

#### 16. 指令下发超时，你会直接重试吗？

网络超时不代表没有执行。先用同一 commandId 重试投递或查询，端侧按编号去重。如果要重新尝试业务动作，必须先确认旧动作终止，再创建新 attempt，避免同时执行。

#### 17. 能保证 exactly-once 吗？

不宣称物理动作绝对只执行一次。数据库和收件箱能去重，但动作完成与落盘之间可能断电。无法确认时进入 UNKNOWN，通过状态对账或人工确认恢复，避免盲目重放。

#### 18. 怎么判断任务真的完成了？

对照批准的计划快照，核对必需步骤的证据：导航到点、讲稿版本和播放完成等。下发成功、收到请求、任务线程退出都不等于业务完成。缺口进入 PARTIAL，只补未完成部分。

#### 19. 取消返回成功，机器人还在动怎么办？

区分取消申请接受和实际停止完成。任务先进入 CANCELING，等待底层反馈并核对状态。超时进入人工处理与本地安全流程，不能把取消请求返回值直接映射成 CANCELED。

#### 20. 中央服务重启如何恢复？

从数据库恢复未终结任务和 Outbox，查询端侧命令账本与物理状态，按版本对账。不能把所有 RUNNING 任务重新下发一遍，也不能因内存清空就释放全部机器人。

#### 21. g1_base 已经有锁，为什么平台还要锁？

g1_base 的锁保护单机器人进程内动作互斥；平台控制权管理不同用户、任务和平台实例的业务归属。它们作用范围不同，平台不能依赖机器人内部锁来解决重复派单。

#### 22. 两台机器人 ROS2 服务名一样会不会串？

可能，所以多机不仅是 HTTP 带 robotId，还要隔离 ROS2 通信域或正确配置命名空间、TF frame 和网络发现。现有绝对服务名需要按实际部署核对。

### D. 问答与知识

#### 23. 两台机器人同时问答怎么隔离？

会话绑定 robotId、sessionId、turnId 和生成代次，答案只能返回原机器人当前有效会话。工具目标由可信上下文注入，不能让模型随意把机器人编号改成另一台。

#### 24. 问答被打断后旧答案回来怎么办？

取消请求只是尽力而为，真正兜底是递增 generationEpoch。端侧播放前核对代次，旧答案直接丢弃；同时保存固定讲解检查点，问答结束后按规则续讲。

#### 25. RAG 与普通调用 LLM 的区别是什么？

RAG 在生成前检索项目自己的知识片段，把问题和证据交给模型。它能改善项目知识覆盖并提供引用，但仍需要权限、版本和拒答机制，不是消除幻觉的保证。

#### 26. 为什么需要关键词检索？

故障码、型号和专有展项名称要求精确匹配，纯语义召回可能漏掉。关键词与向量分别召回，再按排名融合；中文分词和错误码保留规则需要测试。

#### 27. RAG 查到“机器人支持换电”，能回答 G1 支持吗？

不能直接回答。那可能是其他展品机器人的介绍。检索和回答需要校验 robotModel、文档对象及来源版本；无法确认当前 G1 能力就明确说明，而不是把展厅知识当作本机能力。

#### 28. 文档里有动作标签，模型输出后会执行吗？

自由问答不能执行标签。入库清洗 VHML 等执行内容，问答走纯文本播放通道。动作只来自审核过的任务步骤和白名单资源，避免提示注入变成运动指令。

#### 29. 知识库更新后怎么保证不混版本？

新内容先按文档版本完成清洗、切块和索引验证，完成后发布。运行任务固定讲稿版本，新问答按有效知识版本检索；删除和权限变更要及时使旧内容不可检索。

#### 30. 当前电量能通过 RAG 查询吗？

不应该。电量、位置和任务进度从实时状态工具取；RAG 查“低电量应该如何处理”等知识。动态事实与静态资料分开，避免陈旧信息影响调度。

### E. 工程取舍与追问

#### 31. MCP 能保证任务完成和重试安全吗？

不能。MCP 统一工具访问，长流程仍需要任务状态、命令幂等和结果核对。当前机器人接口还需要验证协议兼容性，不能只凭路径名认定标准兼容。

#### 32. 为什么不把任务都放进 ChatMemory？

ChatMemory 为模型提供对话上下文，不是任务执行账本。任务、命令、审批和控制权必须持久化并有约束，不能依赖可能被裁剪、总结或丢失的聊天历史。

#### 33. 系统断网后还能工作吗？

按明确降级策略工作：端侧安全控制保留，不启动新远程步骤，缓存事件；固定音频若已准备可使用。依赖外部 ASR/LLM/TTS 的能力不承诺离线可用，恢复后先对账。

#### 34. 如何评价方案效果？

分别衡量解析正确率、计划审批修改率、有效任务完成率、重复执行、人工接管、问答有据率和端到端延迟。需要真实日志和测试报告后才报数字，不能引用课程收益当本项目成果。

#### 35. 你最核心的技术难点是什么？

不是调用一次模型，而是把不确定的语言理解接到有物理副作用的长流程上：前面限制计划与权限，中间保证资源唯一归属，后面处理取消、重复、断网和结果核对。真实机器人控制经验能支撑这条设计。

#### 36. 如果只给你一期时间，哪些不做？

不做强化学习、自动训练、复杂图谱和自主跨楼层。先把人工任务、可靠执行、状态反馈和安全接管做扎实，再增加自然语言入口和公开知识问答。

### F. G1 与机器狗协同补充

#### 37. 中央平台负责导航和避障吗？

不负责。中央解析接待需求，规划业务步骤并分配设备，例如“G1 去具身智能展项并讲解”；G1 如何到达、如何避开障碍由 g1_base 完成。中央消费到点或失败结果，决定是否推进下一业务步骤。

#### 38. 如何实现 G1 命令机器狗拜年？

把互动建模为带依赖的双设备子任务：G1 和机器狗准备完成后，G1 播放口令，收到对应播放完成事件再下发机器狗的拜年技能，动作完成后 G1 接着讲。默认是语音展示加结构化指令，不声称机器狗通过听觉独立理解了口令。

#### 39. 为什么不把两条命令同时发出去？

因为设备准备、网络和播放时长都不同，同时发送不能保证表演顺序。通过 interactionId 关联命令，用就绪屏障和完成事件推进，并设置超时、去重和取消检查，避免狗先表演、G1 后说话。

#### 40. 讲解正常，但机器狗故障，任务算成功吗？

根据审批计划中互动的必选/可选属性判断。必选互动失败则任务有缺口，需要人工处理；可选互动可以按批准规则跳过并记录，不能把跳过写成表演成功。停止和恢复分别确认两台设备状态。

## 十七、证据索引与待确认事项

### 17.1 机器人源码定位

相对于 `D:/Code/bot_mind`：

| 路径 | 本文使用的依据 |
|---|---|
| `g1_base/g1_base/g1_control_server.py` | 导航 Action、控制互斥、状态与停止 |
| `g1_base/g1_base/unitree_sdk_bridge.py` | 子进程 JSON IPC、速度与停止相关处理 |
| `g1_base/g1_base/nav_core.py` | 导航和本地运动协调 |
| `bot_mind/src/service/g1_control_client.py` | ROS2 控制客户端及结果等待 |
| `bot_mind/src/mcp/http_transport.py` | 当前 JSON-RPC 接口和兼容性边界 |
| `bot_mind/src/mcp/service.py` | 工具发现、调用与展厅上下文 |
| `bot_mind/src/mcp/tools/navigate_to.py` | 根据已配置点位导航 |
| `bot_mind/src/mcp/tools/booth_show.py` | 当前展项讲稿加载与讲解 |
| `bot_mind/src/api/voice.py` | 播放接口与异步返回边界 |
| `bot_mind/src/voice/app/engine.py` | ASR 回调及外部通信入口 |
| `bot_mind/src/voice/app/dialogue_coordinator.py` | 打断及对话状态协调 |
| `bot_mind/src/voice/infrastructure/rust/rust_client.py` | Rust 服务通信线索 |
| `bot_mind/src/service/data_import_service.py` | 展厅资源与知识文件导入 |
| `bot_mind/data/IMPORT_API.md` | 文件导入不等于索引完成 |
| `bot_mind/config/halls.yaml` | 楼层、地图、点位和资源配置 |
| `bot_mind/resources/halls/gansu_5g_1f/speakdoc` | 真实展项讲稿及动作标签 |

### 17.2 巡检源码定位

相对于当前项目 `D:/Code/raccoon-inspection`：

- `raccoon-cloud/raccoon-cloud-drone/src/main/java/com/raccoon/cloud/drone/dispatch/agent/KnowledgeAugmentedPlanningAgent.java`：确定性流程先执行，LLM 工具调用补充解释。
- 同模块 `dispatch/service/TaskGenerateService.java`：解析、状态、知识、分配与下发的流程组织。
- 同模块 `dispatch/service/AuctionAssignService.java`：候选过滤与启发式选择，不把类名等同于全局最优算法。
- 同模块 `dispatch/service/TaskDispatchService.java`：`dispatchToEdge` 当前为日志式接入点，不能证明实际边缘投递完成。
- 同模块 `dispatch/algorithm/PpoSimulationModel.java`：启发式奖励计算，不是真实 PPO 训练推理。
- 同模块 `closeloop`：结果回收、完整性核对和补偿流程的借鉴来源。
- `raccoon-cloud/raccoon-cloud-agent/src/main/java/com/raccoon/cloud/agent/ai/rag/service/HybridRagService.java`：向量和图信息聚合，不等同于本文设计的词法混合检索。

这些源码帮助确定可以复用的思想和接口边界；本文没有对两个工程进行完整运行验证。

### 17.3 面试前优先补齐的信息

1. 原 Java 平台仓库、接口协议、数据库、机器人注册和身份认证方式。
2. 当时真实部署机器人型号、数量、楼层与网络隔离方式。
3. 外部 Rust/ZeroClaw/LLM 的实际进程、配置及调用关系，提供脱敏信息即可。
4. ASR、TTS、模型服务的部署位置及网络依赖。
5. 机器人真实可用的停止、定位、电量与播放完成信号。
6. 你实际负责、共同负责与仅参与讨论的内容边界。
7. 实际测试结果、故障案例和验收材料；没有就不填写性能提升数字。

**复习顺序：先掌握第一、四、五、七、九章，把整条业务链讲通；再看第六、八章理解可靠性；最后用面试题检查能否回答追问。无需一次背完全部表名。**
