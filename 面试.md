# 项目面试速记

> 用于面试前复习，只保留高频问题、项目事实和容易答错的边界。学习过程中的详细推导不持续写入本文档；理解一个新模块后，再统一压缩为最终版本。

## 目录

- [云端基础模块](#云端基础模块)
  - [字典模块｜重要度：中低](#字典模块重要度中低)
  - [统一身份、接口鉴权与单点登录｜重要度：高](#统一身份接口鉴权与单点登录重要度高)
- [巡检功能服务](#巡检功能服务)
  - [巡检业务闭环｜重要度：高](#巡检业务闭环重要度高)
- [无人机边缘侧开发](#无人机边缘侧开发)
  - [端—边—云自主巡检链路｜重要度：高](#端边云自主巡检链路重要度高)

---

## 云端基础模块

### 字典模块｜重要度：中低

#### 一句话

字典模块是系统的固定选项配置中心：适合承载巡检对象类型（杆塔、变压器、断路器）、任务来源等低频变化的选项，以及名称、排序、颜色和图标等展示配置；数据库保存 `value`，页面展示 `label`。

#### 核心模型

| 表/字段 | 含义 | 示例 |
|---|---|---|
| `sys_dict_type` | 管理一整组字典 | `user_source` / 用户来源 |
| `sys_dict_data` | 管理该组的具体选项 | 短视频平台 / `4` |
| `dict_label` | 给用户看的文字 | 短视频平台 |
| `dict_value` | 业务表实际保存的值 | `4` |
| `sort` | 页面展示顺序 | `1`、`2`、`3` |
| `dict_extend` | 图标、颜色等展示扩展 | `{"color":"#52c41a"}` |

示例：用户表保存 `source=4`；页面通过 `user_source` 字典把 `4` 显示为“短视频平台”。按来源筛选时可直接查询 `WHERE source = 4`。

#### 高频问答

**问：为什么要字典模块？**

答：集中维护低频变化的选项，一处配置、多处复用。修改“禁用”为“停用”、调整颜色或顺序时不需要改前后端代码。

**问：前端如何获得映射？会直接查数据库吗？**

答：不会。前端调用后端字典接口，后端先查 Redis、未命中再查数据库，返回 `label/value` 列表；前端显示 `label`，提交 `value`。

**问：既然页面直接传 `dict_type_code`，为什么还要类型表？**

答：单次查询确实只需要数据表。类型表用于登记合法编码、维护中文名称和说明、让管理员下拉选择类型而非手输，并承载整组字典的状态、租户和审计信息。小项目没有管理需求时可以只用数据表。

**问：类型状态和数据状态的区别？**

答：类型状态控制整组，例如禁用 `payment_method` 后整组支付方式不应返回；数据状态控制单项，例如只禁用银行卡。当前项目只校验了数据状态，类型禁用尚未真正拦截查询。

**问：为什么不用枚举？**

答：枚举负责稳定的业务规则和状态流转；字典负责展示和非核心选项配置。新增“用户来源”通常只需增加字典数据；新增真实支付渠道或订单状态则仍需修改后端业务逻辑。不能把已使用的 `value=2` 从“女”改成“中性”，这会改变历史数据含义，应新增 value 并按需迁移数据。

**问：缓存策略是什么？**

答：Cache Aside：读时先查 Redis，未命中查 MySQL 并回填；新增、修改、删除后清理对应缓存；空结果短暂缓存以防缓存穿透。

#### 当前项目事实

- 已完成：类型/数据 CRUD、唯一性校验、逻辑删除、管理员权限、Redis 缓存、按类型查询接口和 `DictSelect` 公共组件。
- 预置：`user_status`、`gender`、`order_status`。
- 尚未完成：用户、设备等主要业务页面仍保留本地硬编码，尚未实际接入 `DictSelect`。
- 可指出的优化：`GET /data/{id}` 与 `GET /data/{dictTypeCode}` 路径可能冲突；类型禁用时应同步阻断数据查询并处理缓存。

#### 30 秒回答

> 字典模块用于统一管理巡检系统中低频变化的通用选项，例如巡检对象类型（杆塔、变压器、断路器）、任务来源，以及标签的颜色和图标等展示配置。它通过 `dict_type_code` 对字典分类，数据表保存 label/value 映射；业务表保存 value，页面展示 label。查询使用 Redis 缓存，修改后删除缓存。字典主要解决硬编码和展示配置问题，核心业务状态流转仍应由枚举或状态机控制。

### 统一身份、接口鉴权与单点登录｜重要度：高

#### 简要开发说明

**目标：** 为 Web 管理端和移动巡检端统一提供登录认证、RBAC 权限校验、多端会话管理和强制下线能力。采用 **Sa-Token + Redis 有状态会话**：Sa-Token 维护 Token 与 `loginId` 的映射、Account-Session、终端信息及踢下线状态；接入 Redis 后，这些状态由 `SaTokenDao` 自动保存到 Redis。业务代码使用 Sa-Token API，不直接拼接或删除框架的 Redis Key。Account-Session 是 Sa-Token 自身的账号级会话，不是 Spring Boot 或 Spring Session 提供的 Session。

```text
账号密码校验成功 / 企业 SSO 返回可信外部身份标识
        ↓
SSO 场景通过 provider + external_user_id 映射本地 userId
        ↓
校验本地账号状态，调用 StpUtil.login(userId, loginModel)
        ↓
Sa-Token 在 Redis 保存登录状态；返回 Sa-Token
        ↓
前端在请求头携带 Token
        ↓
Sa-Token 校验登录态 → 查询 RBAC 权限 → 访问业务接口
```

**核心配置：** 最长登录 7 天、无操作 30 分钟失效、正常操作自动续签；登录时由客户端或业务层向 Sa-Token 提供 `deviceType`、`deviceId` 等终端标识，Sa-Token 负责保存和查询，但不会自动识别真实物理设备。有权限的后台人员可按用户全端 `kickout`，也可根据 `deviceId` 找到对应 Token 后只踢指定终端。修改密码、账号禁用、权限重大变更时执行全端下线。敏感操作（删除巡检任务、修改关键设备配置）使用二级认证。

**权限模型（完整 RBAC）：** 登录、注册、SSO 发起与 SSO 回调接口必须在全局拦截器白名单中放行；认证成功后的人员业务接口才执行全局登录校验。MySQL 使用六张表：`um_user`、`um_role`、`um_user_role_rel`、`um_permission`、`um_role_permission_rel`、`um_third_party_user`；Redis 只保存 Sa-Token 的 Token 和会话状态。用户可拥有多个角色，角色拥有多个权限码。发布人、仓库管理员、工单审核领导不是同一个 `admin` 角色，而是不同角色；角色通过各自权限码访问不同接口。

| 角色示例 | 典型权限码 | 说明 |
|---|---|---|
| 系统管理员 | `system:user:manage`、`system:role:manage`、`system:dict:manage` | 管理账号、角色、字典等系统配置 |
| 任务发布/调度员 | `inspection:plan:manage`、`inspection:task:dispatch` | 制定计划、生成任务、下发巡检工单 |
| 仓库管理员 | `cmms:warehouse:manage`、`cmms:spare-part:manage` | 管理仓库、备件出入库和库存 |
| 工单审核员 | `inspection:work-order:audit` | 审核巡检工单、决定通过或驳回 |
| 巡检员/维修员 | `inspection:work-order:execute` / `cmms:work-order:handle` | 执行本人任务或处理本人维修工单 |

接口策略：全局拦截器统一执行登录校验；多数业务方法用 `@SaCheckPermission` 校验操作权限；`@SaCheckRole` 仅用于平台超级管理员等“必须是特定身份”的少数接口。工单详情、执行、采集等接口即使权限通过，业务层仍须校验当前用户是否为该工单执行人/处理人，即数据权限。前端路由/按钮权限仅改善体验，不能替代后端校验。

**单点登录与统一身份：** 认证与授权职责分离。用户首次访问巡检系统时跳转钉钉、企业微信或企业 IdP；统一平台只负责确认“你是谁”，认证成功后向本项目提供可信的外部身份标识，例如员工工号、`unionId` 或 OIDC 的 `sub`，而不是本项目的用户 ID。本项目以 `provider + external_user_id` 查询 `um_third_party_user` 映射表，得到内部 `um_user.id`；校验本地账号状态正常后，再查询本系统角色权限并调用 `StpUtil.login(localUserId)` 建立 Sa-Token 登录态。姓名不能作为绑定依据，因为可能重复或变更。若平台拆分为管理端、告警中心、报表中心等多个系统，则统一登录中心只认证一次，各子系统分别映射本地用户并建立自己的业务会话。本项目负责“你能做什么”；Sa-Token 负责本系统 Token、Account-Session、终端管理与踢下线，不保存第三方密码。

**预期优化边界：** 当前项目已有机器人遥测上报接口，但尚未实现独立设备鉴权。后续人员操作走 Sa-Token；无人机遥测、视频流、设备高频上报应使用设备证书、设备密钥或 MQTT 鉴权，不能让每条设备数据都走人员登录会话。

#### 高频问答

**问：什么是认证、授权和鉴权？本项目各自在哪里完成？**

答：认证是确认“你是谁”，由账号密码登录或企业 IdP 回调完成；授权是确定“你能做什么”，由用户—角色—权限的 RBAC 数据完成；鉴权是每次接口请求时实际执行登录和权限校验。项目用 Sa-Token 拦截器及 `@SaCheck...` 注解完成鉴权。

**问：用户从登录到访问巡检任务接口的 Sa-Token 完整链路是什么？**

答：账号密码登录时，后端校验密码和账号状态；SSO 登录时，企业 IdP 完成认证，项目根据外部身份映射出内部 `userId`，并校验本地账号状态。随后调用 `StpUtil.login(userId, loginModel)`；Sa-Token 通过 `SaTokenDao` 在 Redis 建立登录状态，前端保存 Token 并在请求头携带。访问任务接口时，拦截器先校验登录态，接口注解再校验角色或权限码，均通过才进入业务代码。

**问：为什么选 Sa-Token + Redis 有状态会话，而不是纯 JWT 双 Token？**

答：巡检后台需要账号禁用、改密码全端失效、管理员踢人和多端终端管理。纯 JWT 已签发后在到期前难立即撤销；Redis 有状态会话可立即改变服务端登录状态。Sa-Token 已封装会话、踢人、多端和权限校验，开发量较小。若需要开放平台或长期移动端无感登录，可再补充 Refresh Token 轮换。

**问：`timeout`、`active-timeout`、自动续签分别解决什么问题？为什么仍要限制最长登录时间？**

答：`timeout` 是最长登录寿命，例如 7 天；`active-timeout` 是无操作超时，例如 30 分钟；自动续签让正常操作的用户不因短活跃期频繁登录。最长寿命仍必须存在，避免被盗 Token 因持续请求无限有效。

**问：Sa-Token 的 Redis 中最基础保存什么？什么时候需要账号会话和终端列表？**

答：最基础是 `Token → loginId`，用于识别登录人。为了支持一个账号关联多个 Token、查询登录终端、限制同时在线数量和按端下线，Sa-Token 还会维护 Account-Session 与终端信息；被踢下线时还会保存相应状态。集成 Redis 后由 `SaTokenDao` 自动持久化这些数据，业务代码应调用 Sa-Token 标准 API，不应自行拼接、删除 Redis Key。Account-Session 是 Sa-Token 的账号级会话概念，与 Spring Session 无关。

**问：Sa-Token 会自动识别手机、浏览器等真实设备吗？**

答：不会。`deviceType`、`deviceId` 等原始终端信息通常由客户端或业务层在登录时采集并传入 `SaLoginModel`，Sa-Token 负责把它们关联到登录会话，并支持后续查询。服务端仍要校验这些信息，不能把客户端自行上报的设备标识直接当作可信身份凭证。

**问：`logout`、`kickout`、`replaced` 的区别是什么？踢人下线为什么不能手工删除 Redis Key？**

答：`logout` 是本人主动退出；`kickout` 是管理员强制下线；`replaced` 是新登录按互斥策略顶掉旧登录。`kickout` 会让旧 Token 立即失效，但通常保留 `KICK_OUT` 状态直到该 Token 原有 TTL 到期，使后续请求可以识别“被踢下线”，而不是普通未登录或过期。应调用 `StpUtil.kickout`，让框架统一处理 Token 状态、Account-Session、终端列表和事件通知；手删 Redis Key 容易留下关联记录，也无法准确区分退出原因。

**问：如何实现修改密码、账号禁用后全端立即失效？如何只踢掉一台手机？**

答：修改密码或禁用账号后，按用户 ID 执行全端 `StpUtil.kickout(userId)`，旧 Token 下次请求立即失效。只踢一台设备时，从该账号的终端会话中根据 `deviceId` 找到对应 Token，再调用 `StpUtil.kickoutByTokenValue(token)`。单纯 `kickout` 只终止当前登录态，并不禁止用户重新登录；账号禁用必须先修改业务数据库中的账号状态，再执行全端下线，并在以后每次账号密码登录或 SSO 回调时继续校验账号状态，否则用户仍能获得新 Token。

**问：为什么本项目需要完整 RBAC，而不只用一个管理员角色？为什么不能只做前端权限？**

答：任务发布人、仓库管理员、工单审核领导都属于后台运营人员，但职责不同。若都赋予 `admin`，仓库管理员可能下发任务，审核员可能修改角色，违反最小权限原则。因此采用“用户—角色—权限”模型：角色聚合权限码，接口按权限码放行。前端隐藏菜单或按钮仅优化体验，攻击者可绕过页面直接请求接口，因此后端必须做最终判断。工单执行还要做执行人/处理人归属校验，防止同角色人员操作他人的工单。

**问：Token 放 localStorage 或 Cookie 有什么安全差异？如何分别防 XSS 和 CSRF？**

答：localStorage 可被页面 JavaScript 读取，重点防 XSS：避免不安全 HTML 渲染、富文本白名单过滤、CSP 和短 Token 生命周期。Cookie 可设 `HttpOnly` 防 JavaScript 读取，但浏览器会自动携带，重点防 CSRF：`Secure`、`SameSite`、CSRF Token 及校验 `Origin/Referer`。移动端通常走请求头 Token，不依赖 Cookie。

**问：Sa-Token 支持手机端、第三方登录和企业统一身份平台吗？OAuth2、SSO 各解决什么问题？**

答：支持。手机端在请求头传 Token 即可。第三方或企业 IdP 完成身份认证并返回可信外部标识后，项目通过映射表找到本地用户，检查本地账号状态，再调用 `StpUtil.login` 创建本系统会话。OAuth2 主要解决第三方“授权访问资源”，SSO 解决多个内部系统“一次登录、多个系统访问”；SSO 负责认证，业务系统负责本地角色权限，Sa-Token 负责本系统登录态和鉴权。

**问：SSO 的一次登录、多系统访问在本项目中如何落地？**

答：用户首次访问子系统时跳转统一登录中心；登录中心认证成功后返回可信身份凭证及员工工号等外部唯一标识。子系统以 `provider + external_user_id` 查询映射表，得到本地 `um_user.id`，检查账号状态，加载本系统角色权限并创建本地 Sa-Token 会话。后续访问其他子系统时，统一登录中心已有登录态，无需再次输入密码。SSO 登录中心并不直接决定本项目能操作哪些接口；权限仍由本项目 RBAC 决定。退出或管理员强制下线时，需要按设计同步注销各子系统会话。

**问：Sa-Token 与 Spring Security 的选型边界是什么？什么时候更倾向 Spring Security？**

答：中后台、IoT 管理平台需要快速落地登录、RBAC、多端与踢人时，Sa-Token 更轻量直接。若深度依赖 OAuth2/OIDC 资源服务器标准、复杂企业身份协议和 Spring Security 生态集成，优先 Spring Security。两者都能做安全，关键看团队经验和企业平台协议要求。

**问：Redis 故障、权限变更、多人同时登录、Token 泄露时如何处理？**

答：Redis 需高可用、监控、持久化和独立鉴权库；鉴权 Redis 不可用时敏感接口默认拒绝而非放行。权限重大变更后清理权限缓存或全端下线；多人登录按业务配置允许多端、限制数量或互斥顶人。Token 泄露时立即踢下线、记录异常日志，缩短活跃期，并对敏感操作加二级认证。

**问：为什么人员鉴权与无人机/设备高频上报鉴权要分开？**

答：人员接口频率低，适合 Sa-Token 的账号、角色和权限模型；无人机遥测、视频或状态上报频率高，若每条都查用户会话会给 Redis 和鉴权链路施压，也不符合设备身份语义。设备应使用设备证书、设备密钥、MQTT 鉴权或服务凭证，并限定设备可上报的主题和资源。

**问：如果面试官问哪些接口用 `@SaCheckLogin`、`@SaCheckRole`、`@SaCheckPermission`，如何回答？源码中实际对应了吗？**

答：登录、注册、SSO 回调、健康检查和公开字典查询放行；其余人员业务接口由全局 Sa-Token 路由拦截器校验登录，相当于 `@SaCheckLogin`。发布计划、下发任务/工单、仓库和备件管理、工单审核、工单执行等方法主要使用 `@SaCheckPermission`，分别要求如 `inspection:task:dispatch`、`cmms:spare-part:manage`、`inspection:work-order:audit`、`inspection:work-order:execute` 的权限码；权限码由角色间接拥有。`@SaCheckRole` 不作为普通业务接口的主方案，仅保留给用户/角色权限体系管理等少数必须是系统管理员的接口。工单详情、执行、采集还必须在 Service 校验当前登录人是否是该工单的执行人或处理人。当前源码**没有** Sa-Token 注解，尚未接入 Sa-Token；但已具备 `um_user`、`um_role`、`um_user_role_rel`、`um_permission`、`um_role_permission_rel` 等模型。字典类型、字典数据的新增、编辑、删除和分页查询目前使用 `@PreAuthorize("hasRole('admin')")`，而字典列表和按类型查询未标注权限；`SecurityConfig` 仍是开发放行配置，因此这不是可直接对外上线的完整鉴权链路。

---

## 巡检功能服务

### 巡检业务闭环｜重要度：高

#### 一句话

巡检功能服务以设备台账为基础，把“什么时候检查、由谁检查、检查哪些项目”生成一次具体任务，再转成可按步骤执行的巡检工单；手机端或无人机回传结果后自动判断异常，异常继续生成维修工单，最终形成“计划—执行—异常—维修—归档”的闭环。

#### 先用一个真实例子理解这个模块

假设厂区里有一台 **1 号变压器**。运维要求每天上午 9 点检查一次，检查内容包括：

- 外壳有没有破损；
- 温度是否处于 20～80℃；
- 有没有异常声音；
- 拍一张现场照片留档。

如果没有巡检系统，管理员可能每天在微信群里通知员工，员工检查后再填写 Excel。这样容易忘记、漏检，也无法确认员工是否真的到场，发现异常后还可能没人继续处理。

巡检功能服务就是把这件事拆成系统能够管理的数据：

| 现实中的事情 | 系统中的对象 | 示例数据 |
|---|---|---|
| 厂区里有一台设备 | 设备台账 | `1号变压器，deviceId=1001` |
| 设备要检查哪些内容 | 巡检点/检查项 | `温度，范围20～80℃`；`外观，必须拍照` |
| 每天上午 9 点检查 | 巡检计划 | `每天09:00检查设备1001` |
| 今天这一次检查 | 巡检任务 | `2026-09-08 09:00检查1号变压器` |
| 发给执行者的作业单 | 巡检工单 | `工单XJ20260908001，执行人张三` |
| 到现场具体做什么 | 工单步骤 | `到达设备→扫码→测温→拍照→提交` |
| 实际检查出了什么 | 巡检记录 | `实测温度92℃，异常，照片URL` |
| 异常由谁修、是否修好 | 维修工单 | `检查散热系统→维修→领导验收` |

这里最关键的理解是：**巡检模块不是控制无人机的模块，它首先是一套“安排检查、记录执行、处理异常”的业务管理系统。** 执行者既可以是拿手机的巡检人员，也可以是无人机或机器狗。

#### 一次人工巡检到底怎么运行

以刚才的 1 号变压器为例：

1. 管理员先在设备台账中录入 1 号变压器，并给它配置温度、声音和外观等检查项。
2. 管理员创建“每天上午 9 点巡检”的计划。计划只是一条长期规则，本身不代表今天已经有人执行。
3. 系统到时间后根据计划生成今天的巡检任务，并生成包含具体操作步骤的巡检工单。
4. 调度员把工单派给张三。张三手机上的“我的任务”出现一条待执行工单。
5. 张三到达现场，系统记录 GPS 签到；扫描设备二维码，确认他检查的确实是 1 号变压器。
6. 张三按顺序填写温度、异常声音、外观和照片。后端把实测温度 92℃ 与上限 80℃ 比较，自动标记异常。
7. 张三提交后，巡检工单变成已完成，系统生成巡检记录。同时因为存在温度异常，系统创建维修工单。
8. 维修人员检查散热系统、填写维修结果，审核人员验收通过后，维修工单关闭。至此这次异常才真正闭环。

因此，“完成巡检”不等于“问题处理完了”：巡检负责发现和记录问题，维修流程负责解决问题。

#### 如果执行者换成无人机会发生什么

计划、任务、工单、异常和归档这些上层业务都不需要推倒重来，主要变化是“如何执行工单”：

| 环节 | 人工巡检 | 无人机巡检 |
|---|---|---|
| 接收任务 | 手机端查看我的工单 | 边缘端拉取已审核的无人机任务 |
| 到达设备 | 人步行到现场 | 云端规划路线，无人机按航点飞行 |
| 确认对象 | 扫描设备二维码 | 拍照航点绑定 `deviceId`，GPS 确认位置 |
| 采集数据 | 人工填温度、拍照片、写备注 | 相机、热成像、声音、振动和温度传感器自动采集 |
| 判断异常 | 后端阈值判断或人工选择 | 边缘 AI 初筛，云端进一步分析 |
| 提交结果 | 手机调用提交接口 | 边缘端批量回传任务结果 |

也就是说，**巡检功能服务负责“这次检查的业务生命周期”，无人机边缘侧负责“怎样让无人机安全完成这次检查”。** 两个模块在巡检任务、路径规划 ID、无人机 ID、设备 ID 和结果数据上完成交接。

#### 为什么要同时设计计划、任务和工单

第一次接触时最容易觉得这三个对象重复，可以这样记：

```text
计划 = 闹钟规则：每天 9 点响
任务 = 今天响的这一次：9 月 8 日这次必须做
工单 = 这一次的操作说明书：谁做、去哪、先做什么、后做什么
```

如果只有计划，就无法单独记录某一天有没有完成；如果只有任务，检查步骤全写死在任务表里会越来越复杂；把工单及步骤独立出来，才能记录派发、执行顺序、照片、实测值和每一步结果。

#### 后端在这条流程中真正做了什么

- 保存设备、检查项、计划、任务、工单和记录之间的关系。
- 使用定时任务把周期计划转换成一次性的巡检任务。
- 使用状态机限制操作顺序，防止已完成工单再次执行或未审核工单直接下发。
- 校验当前用户是不是工单执行人，避免只靠前端隐藏按钮。
- 对实测值与阈值进行比较，并在异常时可靠创建维修工单。
- 通过事务、唯一键和幂等号避免重复任务、重复步骤和重复维修工单。
- 为设备形成长期履历：什么时候检查过、结果是什么、出现过什么故障、怎样修好的。

#### 先分清核心对象

| 对象 | 回答的问题 | 项目中的作用 |
|---|---|---|
| 设备台账 `device_info` | 检查谁？ | 保存设备编号、名称、分类、位置、责任人和运行状态，是设备的数字身份证 |
| 巡检点 `inspection_point` | 检查什么？ | 定义温度、压力、振动等检查项，以及单位、标准值和上下限 |
| 巡检计划 `inspection_plan` | 多久检查一次？ | 保存周期、设备范围、执行人和生效时间，是重复生成任务的规则 |
| 巡检任务 `inspection_task` | 这一次何时由谁执行？ | 计划生成的一次执行实例，也可以手动创建；记录执行人、计划时间、签到位置和总体结果 |
| 巡检工单 `inspection_work_order` | 这一次具体怎么执行？ | 可下发的现场作业单，记录区域、班次、执行人、起止时间和总体状态 |
| 工单步骤 `inspection_work_order_detail` | 先做哪一步、采什么值？ | 按顺序保存 `path/stop/collect/report` 步骤、关联设备、阈值、实测值、照片和异常标记 |
| 巡检记录 `inspection_record` | 最终检查结果是什么？ | 按任务、设备、巡检点归档结构化结果，形成设备历史履历 |
| 维修工单 `work_order` | 发现异常后怎么处理？ | 保存故障描述、照片、维修人、紧急程度及待处理—维修—验收—完成流程 |

最容易混淆的是：**计划是重复规则，任务是某一次实例，巡检工单是这一次实例的执行说明书，维修工单是发现异常后的处理单。**

#### 完整业务流程

```text
设备分类 → 设备台账 → 为设备配置巡检点和阈值
                         ↓
管理员创建巡检计划 → 到期生成巡检任务 → 生成/绑定巡检工单
                                           ↓
                                  手动或定时下发给执行人
                                           ↓
手机端接单 → GPS 签到 → 扫码确认设备 → 按顺序执行步骤
                                           ↓
                           采集数值、照片、备注并判断阈值
                              ↓正常                    ↓异常
                         完成并归档          自动生成维修工单
                                                   ↓
                                         派单→维修→验收→关闭
                                                   ↓
                                      更新设备状态和生命周期履历
```

手机端采用 Vue3、Vite、Vant 和 Pinia，与 PC 管理端共用后端接口；提供设备台账、巡检任务、巡检执行、异常上报、维修工单和个人中心。现场能力包括 GPS 定位、扫码确认、拍照留证和离线队列，恢复网络后重放未提交的写请求。

#### 状态流转

| 对象 | 主要状态流转 |
|---|---|
| 巡检任务 | 待执行 `0` → 执行中 `1` → 已完成 `2`；超过执行期可变为已过期 `3` |
| 巡检工单 | 待下发 `1` → 待执行 `2` → 执行中 `3` → 已完成 `4`；未执行前可取消为 `5` |
| Agent 巡检工单 | 待审核 `6` → 审核通过后下发；审核驳回 `7` → 重新规划 → 再次待审核 |
| 维修工单 | 待处理 `0` → 处理中 `1` → 待验收 `2` → 已完成 `3`；也可取消为 `4` |

状态不能由前端任意覆盖。后端必须校验“当前状态是否允许执行该动作”，例如已完成工单不能再次采集，只有待下发工单才能下发，只有待下发或待执行工单才能取消。

#### 项目代码对应

- `CmmsDeviceController`：设备分类、供应商、部门、仓库、设备台账和巡检点管理。
- `CmmsInspectionController`：巡检计划、任务生成、任务派发、签到、开始执行、异常上报和结果提交。
- `CmmsInspectionWorkOrderController`：巡检工单创建、下发、取消、我的工单、步骤执行、采集和扫码绑定设备。
- `InspectionTaskDispatchScheduler`：每分钟扫描已经到执行时间且绑定了工单的待执行任务，并自动下发。
- `InspectionWorkOrderService`：使用事务创建主单和步骤，校验步骤必须从 1 连续递增，执行时只允许处理当前步骤，采集值超出上下限时标记异常。
- `InspectionTaskService`：计划生成任务、GPS 签到、扫码核对、提交巡检记录，以及异常时自动创建维修工单。
- `raccoon-mobile`：实现设备、任务、巡检执行、异常上报、维修工单、审核、定位、拍照和离线提交等移动端功能。
- Agent 扩展链路：任务规划 Agent 生成路线和工单后先进入待审核状态；人工审核通过才下发无人机，仿真/执行完成后再回填工单结果。这样 AI 负责给建议，人负责安全决策。

七篇资料中，“设备管理概述”“巡检工单功能开发”“巡检管理模开发”和“巡检手机端功能开发”提供了主要业务设计；两个测试文档验证了计划、任务、工单和手机端填报链路；“巡检数据查询优化”目前正文基本为空，因此查询优化部分以下面的源码访问模式和工程设计为面试口径。

#### 高频面试问答

**问：巡检功能服务解决什么问题？**

答：它把原来依赖纸质表格和个人经验的巡检过程标准化、数字化。系统明确巡检对象、执行人、执行时间、检查步骤和判断标准，并保存签到、实测值、照片和异常处理过程，使漏检、错检和责任不清的问题可控制、可追溯；长期数据还能用于维护策略和设备健康分析。

**问：巡检计划、巡检任务和巡检工单有什么区别？**

答：巡检计划是周期规则，例如“1 号变压器每天巡检一次”；巡检任务是计划在某一天生成的一次实例，例如“9 月 8 日 08:00 由张三巡检”；巡检工单是发给执行人的详细作业说明，包含移动、停靠、采集和上报步骤。三者拆开后，规则可以长期复用，每次执行可以独立追踪，现场步骤也能单独调整和留痕。

**问：巡检工单和维修工单为什么不是一张表？**

答：两者业务目的和状态机不同。巡检工单用于发现问题，核心是按步骤检查和采集；维修工单用于解决问题，核心是派单、维修、备件消耗和验收。异常巡检可以生成维修工单并通过设备 ID、来源任务或异常记录建立关联，但不应把两套状态塞进一个字段，否则流程会混乱。

**问：为什么巡检工单要设计成主表和详情表？**

答：一张工单包含多个有序步骤，是一对多关系。主表保存工单编号、人员、时间和总体状态，详情表保存每一步的设备、检查项、阈值和结果。这样既避免重复保存主单信息，又能灵活增加步骤；通过 `order_id` 查询步骤，并用 `(order_id, step_order)` 唯一约束保证同一工单步骤序号不重复。

**问：系统如何保证巡检人员按顺序执行步骤？**

答：后端按 `step_order` 找到第一个未完成步骤，只允许提交该步骤；`stop` 步骤必须先扫码绑定或核对设备，`collect` 步骤必须走采集接口。完成第一步后再推进下一步；首次执行把主单改为执行中，全部步骤完成后自动记录完成时间并把主单改为已完成。不能只依赖前端步骤条，因为调用者可以绕过页面直接请求接口。

**问：采集值是怎样判断异常并形成闭环的？**

答：巡检点或工单步骤保存 `standardMin`、`standardMax` 和单位；提交实测值后，后端使用 `BigDecimal` 与上下限比较，越界就标记异常。任务完成时如果存在异常记录，则在同一业务事务中标记任务异常并创建来源为“巡检上报”的维修工单，带上设备、异常描述和照片，之后进入维修派单与验收流程。

**问：为什么异常上报要使用事务？**

答：一次异常提交至少会修改任务状态、写入巡检记录并新增维修工单。这些操作要么一起成功，要么一起回滚；否则可能出现任务显示异常却没有维修工单，或者已经创建维修工单但巡检结果没有保存。跨微服务时不能只依赖本地事务，可使用事务消息、可靠事件表和幂等消费保证最终一致性。

**问：定时自动派发怎样避免重复工单？**

答：调度器扫描“待执行、已到计划时间、尚未派发”的任务。生产设计中要使用 `(status, plan_execute_time)` 联合索引，并通过状态条件更新或唯一业务键抢占任务；多实例部署时再配合分布式锁。创建工单和回写 `work_order_id` 必须具备幂等性，即使调度重复执行也只能得到一张有效工单。

**问：工单编号如何保证唯一？**

答：数据库必须有唯一索引，这是最终防线。编号可以采用日期加序列、雪花 ID 或独立号段服务。不能只使用“先查当天最大编号再加一”，因为并发请求可能读取到同一个最大值；若使用这种方式，至少要加锁并处理唯一键冲突重试。

**问：设备扫码和 GPS 签到解决什么问题？这些数据可以完全相信吗？**

答：扫码用于确认当前设备与任务设备一致，GPS 用于证明执行人到达现场，照片用于留证，从而降低漏检和代检风险。但客户端数据可以伪造，所以服务端还要校验任务归属、二维码签名或设备编码、签到时间、坐标与设备位置的距离，并保留审计记录；高风险场景还可结合动态二维码、NFC、电子围栏和照片水印。

**问：手机端离线提交怎么设计？**

答：断网时把写请求及本地业务流水号放入离线队列，联网后按顺序重放。后端必须以业务流水号做幂等校验，防止重复生成记录或工单；重放前检查 Token 是否过期，图片应先可靠上传再提交 URL，还要处理任务已取消、步骤已被别人完成等状态冲突。离线队列只能提高可用性，不能绕过后端权限和状态校验。

**问：巡检列表和工单详情如何优化查询？**

答：先根据真实查询条件建联合索引，例如手机端“我的待办”使用 `(inspector_id, status, id)`，定时派发使用 `(status, plan_execute_time)`，步骤列表使用 `(order_id, step_order)`。列表只查必要字段并分页；详情先查主单，再一次批量查询全部步骤和涉及的设备，避免循环查询造成 N+1。历史数据量大时按时间归档或分区，热点统计可用缓存，但状态变化后要及时失效。

**问：`inspection_plan.device_ids` 保存 JSON 数组合理吗？**

答：小规模项目实现简单，但不利于外键约束、按设备反查计划和索引优化。完整设计更适合增加 `inspection_plan_device_rel(plan_id, device_id)` 关系表，并对二者建立唯一约束。面试时可以说明：JSON 是早期快速实现，数据量和查询复杂度上升后演进为关系表。

**问：`inspection_record` 和 `inspection_work_order_detail` 都保存结果，会不会重复？**

答：工单详情强调现场执行过程和步骤快照，巡检记录强调按任务、设备、巡检点沉淀的结构化历史数据。可以同时存在，但必须明确唯一事实来源：步骤提交先更新工单详情，完成工单时在同一事务中生成巡检记录，或通过可靠事件异步归档；不能让两个入口分别修改同一个结果，否则会产生不一致。

**问：设备遥测数据为什么不直接全部放 MySQL？**

答：设备台账、计划和工单是低频事务数据，适合 MySQL；温度、振动等遥测可能每秒产生大量数据，写入密集且主要按设备和时间范围聚合，适合 Kafka 加时序数据库，例如 InfluxDB 或 TimescaleDB。MySQL 可以保存告警结果和关键摘要，不负责承接全部原始遥测。

**问：巡检接口如何做权限和数据权限？**

答：创建计划、生成任务和下发工单要求调度权限；工单审核要求审核权限；设备和备件管理要求相应管理权限；执行和上报要求巡检或维修权限。通过 `@SaCheckPermission` 只能判断“有没有执行这类操作的资格”，Service 还必须校验当前用户是不是该任务的执行人、该工单的处理人，避免同一角色的员工查看或操作他人的工单。

**问：任务规划 Agent 在巡检业务中的作用是什么？为什么仍要人工审核？**

答：Agent 根据设备、巡检点和现场约束生成路径及工单步骤，提高复杂任务的规划效率。生成结果先进入待审核状态，审核通过后才下发无人机；驳回时保存原因并重新规划。因为巡检涉及生产安全，AI 结果可能存在路线不可行、设备选择错误或安全约束遗漏，所以采用“AI 生成、人审决策、执行回填”的人机协同模式。

#### 60 秒项目回答

我负责的巡检功能服务以设备台账和巡检点为基础。管理员配置周期性巡检计划，系统生成某次巡检任务并绑定巡检工单；工单采用主表加详情表，详情按 `path、stop、collect、report` 保存有序步骤。任务到时间后可以自动下发，巡检人员在手机端接单，通过 GPS 签到和扫码确认设备，再逐项录入数据、照片和备注。后端按上下限自动判断异常，异常会生成维修工单，经过派单、维修和验收后关闭，从而形成完整闭环。实现上使用 MyBatis-Plus、事务和状态机校验保证数据一致性，针对我的待办、定时派发和步骤查询建立联合索引，同时通过幂等设计解决定时任务和离线重放导致的重复提交。项目还扩展了 Agent 路径规划，但生成的无人机巡检工单必须先经过人工审核才能下发。

---

## 无人机边缘侧开发

### 端—边—云自主巡检链路｜重要度：高

#### 一句话

无人机边缘侧是云端巡检平台与无人机飞控之间的现场执行层：它把云端的通用巡检路线转换成具体机型能够执行的 Waypoint 任务，完成飞前检查、自主飞行、多传感器采集和本地异常初筛；网络不好时仍能继续执行并缓存数据，联网后再补传结果。

#### 先把“边缘侧”想成三个角色

第一次接触时，可以把边缘侧理解成无人机旁边的一台现场电脑，它同时扮演三个角色：

1. **翻译员：** 云端只说“去 1～5 号杆塔巡检”，边缘侧把通用路线翻译成大疆等具体飞控认识的 Waypoint 任务。
2. **现场驾驶员：** 它检查电量和 RTK，把任务上传给飞控，监听飞行过程，并在必要时触发返航等本地安全动作。
3. **现场数据站：** 它接收照片、热成像、声音、振动和位置数据，先压缩、降噪和初筛；没网时存本地，有网时上传云端。

云端好比公司总部，知道所有设备、人员和任务；边缘侧好比驻扎在现场的执行人员；飞控好比真正控制电机和姿态的驾驶系统。云端不会每 100 毫秒告诉电机怎样转，边缘侧也不会决定全公司下个月的巡检计划。

#### 用一次杆塔巡检看懂全过程

假设现在要让无人机巡检输电线路上的 1～3 号杆塔。

**第一阶段：云端准备任务。**

巡检服务先产生“检查 1～3 号杆塔”的任务。中央 Agent 或调度服务查看有哪些无人机在线、电量多少、正在执行几个任务，然后选择无人机 1 号。路径服务决定先后访问顺序，避开障碍物和禁飞区，生成一条通用路径：

```text
机巢 → 安全爬升点 → 1号杆塔拍照点 → 2号杆塔拍照点
     → 3号杆塔拍照点 → 返航点
```

它保存的不是“让左侧电机转快一点”这种底层指令，而是业务级 JSON：

```json
{
  "planId": 501,
  "taskId": 2001,
  "uavId": 1,
  "waypoints": ["起飞点", "1号杆塔", "2号杆塔", "3号杆塔", "返航点"],
  "photoWaypoints": [
    { "waypointIndex": 1, "deviceIds": [101] },
    { "waypointIndex": 2, "deviceIds": [102] },
    { "waypointIndex": 3, "deviceIds": [103] }
  ]
}
```

`deviceIds` 很重要，它表示这张照片检查的是哪台业务设备。任务结束后，系统才能把照片和异常准确归档到对应杆塔，而不是只得到一堆无法识别来源的图片。

**第二阶段：人工审核。**

由于无人机巡检涉及生产和飞行安全，Agent 生成的终端选择、路线和预计电耗先进入待审核。审核人员确认设备、路线、时间和安全约束后才能下发；驳回则带原因重新规划。

**第三阶段：边缘端翻译并执行。**

边缘端拉取 `planId=501`，确认任务确实分配给无人机 1 号，然后把每个通用航点转换为飞控能够识别的 Waypoint：经纬度、高度、速度，以及到点后的云台旋转和拍照动作。上传飞控前检查电量、RTK、返航点、传感器、航点数量和电子围栏；检查不通过就不允许起飞。

**第四阶段：飞控自主飞行。**

任务完整上传以后，飞控自己按照 Waypoint 控制姿态和电机。即使云端临时断网，无人机也可以继续执行已经确认的任务。边缘端持续读取飞控状态，但不会通过互联网逐帧遥控飞机。

**第五阶段：到达航点并采集。**

无人机到达 1 号杆塔附近后减速或悬停，云台转到合适角度，采集可见光、热成像、声音、振动和温度。所有数据带上同一个任务 ID、航点序号、设备 ID、GPS 和采集时间。边缘侧先生成缩略图、提取最高温和声振特征，并用轻量 AI 判断是否疑似破损、过热或火情。

**第六阶段：数据上报。**

位置、电量等小数据持续上报；图片和音频等大文件进入对象存储，业务消息只传文件 URL 和分析结果。如果断网，边缘端把数据持久化到本地 SSD，恢复后按“异常优先、普通数据随后”的顺序补传，并依靠业务唯一键防止重复入库。

**第七阶段：任务闭环。**

无人机完成航线后自动返航。云端收到结果后更新巡检工单并生成巡检记录；如果发现 2 号杆塔温度异常，就进入异常分级、复巡或维修工单流程。这样无人机边缘侧最终仍然回到上一章讲的巡检业务闭环。

#### 四个容易混淆的名词

| 名词 | 简单理解 | 示例 |
|---|---|---|
| 航点 Waypoint | 无人机必须到达的空间位置，可附带动作 | 到 2 号杆塔上方 38 米处，云台下转 30°并拍照 |
| 路径规划 | 计算航点的访问顺序和点与点之间怎样飞 | TSP 排顺序，RRT* 绕障碍，平滑算法减少急转弯 |
| 遥测 Telemetry | 无人机自身“现在怎么样” | 位置、高度、速度、电量、RTK、飞行状态 |
| 多模态巡检数据 | 被检查设备“现在怎么样” | 杆塔照片、热成像最高温、声音特征、振动频谱 |

遥测与巡检数据不是一回事：电量 35% 描述的是无人机，热成像 92℃ 描述的是被巡检设备。前者主要用于飞行监控和调度，后者用于判断设备故障。

#### 两个模块到底在哪里交接

```text
巡检服务输出：任务ID、要检查的设备ID、检查要求、审核结果
                         ↓
云端无人机服务输出：无人机ID、路径规划ID、航点和拍照点
                         ↓
边缘侧负责：转换、飞前检查、飞行、采集、缓存和上传
                         ↓
边缘侧回传：轨迹、照片/特征、异常、任务完成结果
                         ↓
巡检服务接回：更新工单、保存记录、生成复巡或维修工单
```

所以它们不是两个互相独立的项目：巡检服务给无人机“业务任务”，边缘侧把业务任务变成“物理执行”，执行结果再回到巡检服务。

#### 为什么不能把所有工作都放在云端

- 无人机控制要求低延迟，若每个动作都等待云端响应，网络抖动会直接影响作业安全。
- 高清图像、热成像、音频和振动原始数据体量大，全部实时上云会占用大量带宽。
- 输电线路、矿山和偏远厂区可能弱网或断网，已经下载到本地的任务必须能够离线完成。
- 设备厂商 SDK、串口/USB、飞控协议与具体硬件有关，放在边缘侧适配，可以让云端保持统一业务模型。

边缘侧不是另一套后台管理系统，也不是数据库中的一张表；它是运行在地面站、边缘网关或机载计算机上的程序及算力。

#### 端、边、云分别负责什么

| 层级 | 主要职责 | 本项目示例 |
|---|---|---|
| 端侧 | 飞行、定位、拍照和传感器采集 | 无人机飞控、相机、热成像、音频、振动和温度传感器 |
| 边缘侧 | 协议适配、飞前检查、自主执行、本地预处理/推理、缓存补传 | 地面站或机载电脑，将路径转为 Waypoint，断网继续巡检 |
| 云端 | 设备台账、工单、全局调度、路径规划、数据归档、报表和模型迭代 | `raccoon-cloud-drone`、`raccoon-cloud-iot-data`、中央 Agent |

```text
管理员/Agent 创建巡检任务并通过人工审核
                 ↓
云端选择终端，规划访问顺序和安全航线
                 ↓
生成通用路径 JSON：任务、无人机、航点、拍照点、预计能耗
                 ↓
边缘端拉取任务 → 校验版本/归属 → 转为厂商 Waypoint 任务
                 ↓
检查电量、RTK、通信、传感器和返航条件 → 上传飞控并启动
                 ↓
无人机自主飞行 → 到点调云台 → 拍照/多模态采集 → 本地异常初筛
                 ↓
实时状态上报；弱网时本地持久化，恢复后按序批量补传
                 ↓
云端保存实时快照、历史轨迹和巡检结果 → 异常分级/复巡/维修闭环
```

#### 三条数据链路要分开

| 链路 | 数据 | 特点与建议 |
|---|---|---|
| 指令下行 | 任务、航线、暂停、返航 | 低频但高风险，必须鉴权、审计、幂等并确认执行结果 |
| 实时遥测 | 位置、高度、速度、电量、姿态、RTK、飞行状态 | 高频小报文；实时快照与历史轨迹应分开处理 |
| 巡检结果 | 图片、热成像、音频特征、振动、温度、AI 结果 | 大文件与结构化元数据分离；异常优先、正常数据批量上传 |

实时快照回答“无人机现在在哪里”，通常只保留每台设备最新一条，可放 Redis 或快照表；历史轨迹回答“它走过哪里”，应批量写入时序库或按时间分区的历史表。图片、音视频原文件应进入对象存储，MySQL 只保存任务关系、文件 URL、特征和检测结果，不能把大量 Base64 原图长期塞进关系库。

#### 路径规划与 Waypoint 转换

云端路径 JSON 是厂商无关的业务协议，保存 `planId/taskId/uavId/mapId`、起降点、航点、拍照点、设备访问顺序和预计距离/耗时/电量。边缘端再把它转换为具体飞控的任务格式，例如：

- 全局设置最大速度、巡航速度、航向模式和完成后自动返航。
- 将经纬度、高度转换为飞控航点；靠近巡检点时减速。
- 拍照航点追加云台旋转和拍照动作，并绑定被巡检设备 ID。
- 检查机型航点上限；超长线路按航段拆分，而不是直接下发超大任务。

转换必须放在边缘侧，因为不同厂商、型号和 SDK 的字段及限制不同。云端只维护统一业务语义，增加新机型时新增适配器，不改动巡检计划和工单模型。

本项目的全局规划流程是：TSP 优化多个设备的访问顺序，RRT* 在障碍/禁飞约束下规划各段三维路径，再进行轨迹平滑，最后计算距离、耗时和预计电量。TSP 解决“先去哪个设备”，RRT* 解决“两个点之间怎么绕开障碍”，轨迹平滑解决“结果能否安全、平稳地飞”。Three.js 中的 Catmull-Rom 曲线主要服务于仿真展示，不能代替真机的安全航线校验。

#### 飞前检查与自主执行

文档和仿真中的基础校验是电量不低于 20%、RTK 必须达到固定解，然后把任务上传飞控。完整生产设计还要校验：无人机与机巢在线、任务与设备绑定正确、航线未越界或进入禁飞区、返航点和返航高度有效、载荷与传感器就绪、天气和剩余续航满足任务需要。检查失败时任务保持未执行并返回明确原因，不能先起飞再补检查。

任务上传飞控后，飞控按 Waypoint 自主执行；云端短时断开不应中断已确认的安全任务。出现低电量、RTK 丢失、通信失联、障碍物或天气异常时，由飞控/边缘安全策略决定悬停、降级、返航或迫降；云端主要记录和告警，不能成为最后一道实时安全控制。

#### 多模态采集与边缘预处理

同一航点可采集可见光、热成像、音频、振动和温度。边缘侧先做畸变/光照校正、降噪、滤波、单位与格式统一，再用采集时间、航点、GPS 和设备 ID 将不同频率的数据对齐。轻量模型只做异常初筛，例如高温、火情、异响或异常振动；云端负责跨任务趋势分析、复杂模型、知识归档和报告。

面试时不要说“边缘侧永远不上传原图”。更准确的说法是：异常结果和缩略图优先上传；原图可按业务合规要求上传对象存储，或者先本地保留、按需补传。边缘压缩也必须保证缺陷特征和证据链不被破坏。

#### 高频上报、弱网与海量数据

若原始遥测是 10Hz，一台无人机一天最多产生 `10 × 86400 = 864000` 个点；100 台就是 8640 万个点，所以“HTTP 收到一条就同步插一次 MySQL”不可扩展。合理链路是：

```text
飞控高频采样
  → 边缘聚合/降采样/压缩
  → MQTT 或批量 HTTP 接入
  → 消息队列削峰
  → 消费者校验、去重并批量写入
  → 实时快照 + 分区/时序历史存储
```

网络断开时，数据必须持久化到本地文件或嵌入式数据库，不能只放内存。每条报文携带 `deviceId + taskId + sequenceNo + collectedAt` 等业务唯一信息；补传采用至少一次投递，云端依靠唯一键/幂等表去重，并根据采集时间和序号处理重复、乱序和迟到数据。上传成功应以云端确认或确认水位为准，再清理本地数据；设置磁盘上限、分级淘汰、校验和及失败重试，异常数据优先保留。

MQTT 适合设备长连接、发布订阅、QoS 和大量小报文；HTTP 适合任务拉取、管理接口和批量结果上传。两者可以并存，不需要二选一。MQTT QoS 1 常用于重要遥测，但它只保证至少一次到达，业务层仍须幂等；飞行控制类命令还需要命令 ID、有效期、签名、状态机和执行回执。

#### 当前仓库中的实际对应

- `raccoon-edge` 已按 `navigation/collection/inference/comm` 划分模块边界，目前主要是 Maven 骨架；可把它理解为真机边缘服务的目标结构。
- `raccoon-drone-sim` 使用 Vue3、Three.js 模拟云端拉取路线、Waypoint 转换、飞前检查、自动飞行、拍照、多模态采集、本地 AI、断网缓存和自动返航，是没有真机时的数字孪生联调环境。
- `UavRoutePlanController` 和 `UavRouteDispatchBuilder` 提供按无人机与规划 ID 拉取标准下发 JSON；`GlobalPathPlanService` 组合 TSP、RRT* 和轨迹平滑生成全局路径。
- `InspectionRobotController` 接收位置、运行态、电量和边缘负载等分通道遥测；`uav_robot_runtime_status` 保存每台机器人的最新状态，15 秒没有有效心跳则判为离线，供 Agent 调度判断。
- `UavLocationHistoryController` 接收轨迹批次，`uav_location_history` 按设备和时间查询，并采用时间分区表设计。
- `UavInspectionMultimodalController` 以一次任务一条 `uav_inspection_session`、多个航点采样 `uav_inspection_sample` 保存多模态结果，并继续归档到图谱/RAG 链路。
- 仿真为避免浏览器和开发环境压力，对部分链路做了降频：历史轨迹默认 1Hz，机器人位置接口实际约 2Hz。面试时应说明“飞控可高频采样，实时展示、历史持久化按用途降采样”，不要声称所有 10Hz 数据都逐条同步写 MySQL。
- 当前 RRT* 已保留障碍检查扩展接口，但默认检查器按无障碍处理；生产版本应接入真实禁飞区、障碍物和电子围栏数据。

文档目录共有 12 个条目，已逐个打开并等待正文加载后完整读取。资料覆盖 3D 数字孪生、端边云架构、自主巡检、实时状态上报、多模态采集与预处理、海量文件上报、MySQL 向 InfluxDB 的时序数据迁移、异构设备管理、路径 JSON 转 Waypoint JSON 以及路径规划。本节同时对照了项目源码；文档描述的是预期完整方案，源码部分用于说明目前仓库中的具体实现和仿真边界。

#### 高频面试问答

**问：什么是无人机边缘侧？这个模块解决了什么问题？**

答：它是运行在地面站、边缘网关或机载计算机上的现场执行服务，位于云端业务系统和无人机飞控之间。它负责厂商协议适配、任务转换、飞前检查、本地执行、多模态预处理、轻量推理和断网缓存，主要解决实时控制延迟、海量数据带宽、弱网自治以及不同设备协议不统一的问题。

**问：本项目无人机巡检的完整流程是什么？**

答：管理员或 Agent 创建任务，人工审核后由云端选择终端，优化设备访问顺序并规划航线；云端生成厂商无关的路径 JSON。边缘端拉取后校验任务归属和版本，转换为飞控 Waypoint，完成电量、RTK、传感器和安全范围检查，再上传并启动任务。无人机按航点飞行，到点完成云台调整、多模态采集和本地异常初筛，同时上报状态；断网时继续执行并缓存。任务结束自动返航，联网后补传轨迹和结果，云端再进行异常分级、复巡或维修闭环。

**问：为什么路径规划在云端，Waypoint 转换却在边缘端？**

答：路径规划依赖全局任务、全部设备、地图、调度策略和历史数据，适合云端统一计算；Waypoint 是具体厂商飞控协议，受机型、SDK、航点数量和动作字段限制，适合边缘侧就近适配。这样云端模型稳定，增加新机型时只实现新的边缘适配器。

**问：TSP、RRT* 和轨迹平滑分别做什么？**

答：TSP 决定多个巡检设备的访问顺序，减少总路程；RRT* 规划两个点之间绕开障碍和禁飞区的三维可行路径，并通过重连逐步降低代价；轨迹平滑删除无意义拐点并降低急转弯。三者分别解决访问顺序、空间避障和可飞性，不是互相替代关系。

**问：为什么不能把 Catmull-Rom 平滑曲线直接当作真机航线？**

答：曲线平滑后可能越出安全走廊、切入障碍物或产生不满足飞行器动力学的转弯。Three.js 中使用它可以让动画自然，但真机必须在平滑后重新做禁飞区、最小距离、坡度、转弯半径、速度和高度约束校验，最终以飞控认可的离散航点为准。

**问：飞前需要检查哪些条件？**

答：基础项包括电量、RTK、通信、任务归属、航点数量、返航点、飞控和载荷状态；生产环境还应检查禁飞区/电子围栏、天气、可见卫星数、存储空间、任务预计电耗和返航余量。检查失败就不启动任务，并记录原因；不能只依赖前端按钮校验。

**问：断网后无人机为什么还能巡检？数据怎样保证不丢？**

答：路线在起飞前已经完整上传到飞控，执行不依赖云端逐点发指令。边缘端将未上报遥测和结果持久化到本地，以序号和采集时间记录进度；网络恢复后按批次补传，收到云端确认后再清理。本地缓存解决可用性，云端幂等、校验和、重试和乱序处理共同解决数据正确性。

**问：断点续传怎样防止重复数据？能做到 exactly once 吗？**

答：网络异常时发送方无法确定云端是否已经处理，所以通常采用至少一次投递，而不是承诺端到端 exactly once。每条数据带业务唯一键，例如 `deviceId + taskId + sequenceNo`，云端建立唯一约束或幂等记录；重复报文直接返回成功，补传端据确认水位删除本地数据。

**问：10Hz 遥测为什么不能逐条同步写 MySQL？怎么优化？**

答：一台设备每天最多 86.4 万个点，设备规模上来后连接、事务、索引和磁盘压力很大。边缘侧先聚合、降采样和压缩，接入层用 MQTT 或批量 HTTP，消息队列削峰，消费者批量写入；实时位置只更新快照，历史轨迹进入按时间分区表或时序数据库，并按保留策略降采样归档。

**问：实时状态表和历史轨迹表为什么要分开？**

答：实时状态按设备 ID 覆盖更新，数据量小，服务于地图展示和调度决策；历史轨迹不断追加，服务于回放、审计和统计，数据量很大，需要设备加时间索引、分区或时序存储。混在一张表会让高频写入、查最新和历史查询互相影响。

**问：为什么将历史轨迹从 MySQL 迁移到 InfluxDB？Tag 和 Field 怎么设计？**

答：轨迹数据按时间持续追加，查询也主要是“某台无人机、某个任务、某段时间”，还需要降采样和自动保留策略，符合时序数据库的访问模型。MySQL 继续保存设备、任务、地图、路径和工单等强关系数据，Redis 保存最新状态，InfluxDB 保存历史时序点。`uav_id`、`task_id`、`map_id` 等低基数过滤维度适合做 Tag；经纬度、高度、速度、电量和姿态等连续值应做 Field，不能做 Tag，否则会导致索引基数过高。

**问：历史数据迁移期间如何保证不丢数据并安全切换？**

答：采用“历史分片迁移、新数据双写、数据校验、灰度切读”的流程。历史表按天或小时分批读取并批量写入 InfluxDB；迁移期间新数据同时写入新旧存储；通过 `uavId + taskId + timestamp` 保证重跑幂等，并按任务和时间窗口进行数量核对与轨迹抽样。确认一致后逐步把轨迹回放和曲线查询切到 InfluxDB，稳定一段时间后再归档 MySQL 历史数据。

**问：边缘侧怎样判断设备在线？**

答：设备周期性发送心跳或运行态，云端保存最后上报时间；`onlineFlag=true` 且最后心跳没有超过阈值才判在线。本项目运行态超过 15 秒未更新视为离线。生产环境要结合不同通道频率设置阈值，避免单次丢包就误判；状态变化还要触发缓存失效和告警。

**问：多模态数据为什么要在边缘侧预处理？**

答：图像、热成像、音频、振动和温度的格式、频率及噪声不同。边缘侧先做校正、降噪、滤波、时间对齐、压缩和异常初筛，可以减少无效数据和上行带宽，让云端直接进行跨任务分析。边缘做快速局部判断，云端做全局、复杂和长期分析。

**问：多模态数据在数据库中怎么设计？**

答：一次巡检任务对应一条会话记录，包含无人机、任务、路径、地图、起止时间和总里程；每个航点的每种模态是一条采样记录，保存模态类型、采集时间、GPS 和结构化载荷。大文件进入对象存储，采样表保存 URL、摘要和特征；这样既能按会话查询，也能按航点把不同模态对齐。

**问：MQTT 和 HTTP 应该怎么选？**

答：高频小报文、设备长连接和状态发布更适合 MQTT；任务管理、路径拉取和批量结果上传更适合 HTTP。MQTT 的 QoS 1 仍可能重复，业务层需要幂等；涉及飞行的下行命令还要有设备级权限、命令 ID、有效期、签名和执行回执。

**问：无人机接口可以直接使用人员的 Sa-Token 吗？**

答：人员管理接口可以使用 Sa-Token，但无人机高频上报应使用独立设备身份，例如设备证书、设备密钥和 MQTT ACL。设备只能访问分配给自己的主题和任务，凭证可以轮换和吊销；危险指令还需要人员权限、审核和审计。人员身份与设备身份不能混成一种 Token。

**问：3D 仿真系统的价值是什么？它等于真机边缘程序吗？**

答：它不等于真机程序，而是数字孪生联调环境。没有实体无人机时，可以验证云端下发 JSON、任务状态机、Waypoint 转换、飞前失败、断网补传、拍照和结果回填，也适合演示和回归测试。真正上线仍要接入厂商 SDK、真实传感器、设备鉴权和安全策略，并通过仿真、半实物和真机逐级验证。

**问：当前源码中哪些是已落地，哪些仍是可扩展设计？**

答：已落地的是 Three.js 无人机仿真、路径 JSON 和 Waypoint 转换、飞前电量/RTK 校验、自主飞行、多模态模拟采集、状态与历史轨迹上报、任务结果入库，以及云端的终端状态、TSP/RRT* 路径规划和 Agent 调度链路。`raccoon-edge` 真机模块目前主要是分层骨架，厂商 SDK、持久化离线队列、MQTT 设备鉴权、对象存储和真实禁飞区碰撞检查属于生产化扩展。面试可以按项目预期架构讲完整方案，但被追问代码时要准确指出仿真与真机的边界。

#### 60 秒项目回答

我负责的无人机边缘侧模块采用端—边—云协同架构。云端根据巡检工单、设备点位和终端状态完成任务分配，通过 TSP 优化访问顺序、RRT* 规划避障路径并做轨迹平滑，生成厂商无关的路径 JSON。边缘端部署在地面站或机载计算机，拉取任务后转换为飞控 Waypoint，检查电量、RTK、航点和返航条件，再上传飞控执行自主巡检。飞行过程中采集位置、电量以及可见光、热成像、声音、振动和温度数据，在边缘侧完成降噪、时间对齐、压缩和异常初筛；实时快照与历史轨迹分开存储，弱网时本地持久化并在恢复后幂等补传。任务完成后自动返航并把结果回传云端，异常继续触发告警、复巡或维修闭环。没有真机时，项目使用 Three.js 数字孪生验证整条链路。
